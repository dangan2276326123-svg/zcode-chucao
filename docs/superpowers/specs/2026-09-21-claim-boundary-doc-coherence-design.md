# 主张边界与文档一致性修订（2026-09-21 开单，09-22 第二版）

> **本批做什么**：调整**验证主张、统计交付口径、准入判据与文档表述**。
> 不增加采集因素、不新增实验维度、不给工具加功能。
> （第一版自称"只改措辞、口径与事实登记"，被 09-22 审查指出不准确：删除按条件统计、
> 取消元数据记录、改变 S0 的触发作用，都属于**验证协议**调整而非文字润色。已改口。）
>
> 基线：qoder `b4e8e5d` 起的第二版；驱动来源为
> `D:\ai work\codex\docs\reviews\2026-09-22-two-specs-claim-boundary-review.md`。
> 该审查的"用户反馈后校准"一节是当前有效结论，本文件吸收其成立部分，
> 并逐条核过其对第一版的六项具体指错——**六项全部核实为真，其中五项是第一版我自己写错的**。

## 0. 作者决定（本版的输入，共七条）

| # | 决定 | 来源 |
|---|---|---|
| 甲 | 不新增实验维度：不做跨田块泛化、季节或光照的分组对比实验，不额外跑田 | 09-21 |
| 2 | 时段、田块**都不逐图记录**；采集维度只作数据集覆盖说明 | 09-21 |
| 3 | V1 不按条件拆分报数 | 09-21 |
| 4 | 失视觉观测并进门 N 已有验收项，不新增第四项 | 09-21 |
| **5** | **场地表述退一步**：不记逐图田块，只在数据集描述写**统一采样区域**；主张用"同一采样区域内"，**不写"同田块"** | **09-22 本版新增** |
| **6** | **V1 两类量分给两个数据集**：误差量→精选标注照片（有独立真值）；失败率与最长连续失效→3 段连续视频（保留顺序、不预筛）；双墙率在两边分开报 | **09-22 本版新增** |
| **7** | **秋季门 N 不等微调**：S0 升回门 N 的**可行性判据**，不过则按既有退路降为遥控采集 | **09-22 本版新增** |

决定 5/6/7 是对第一版三处错误的纠正，见 §2、§3、§5。

## 1. 现场事实与证据等级（第一版把两处抬高了）

| 编号 | 内容 | 等级 | 第一版的问题 |
|---|---|---|---|
| Hw-22 | 作者预期：10 月中目标田芍药地上部仍基本完整（芍药 3–4 年生） | **作者基于当地经验的预期**，非现场复核 | 第一版写"物候前提由作物学依据**关闭**"。**"3–4 年生"推不出"今年这块田 10 月中作物墙可见"**——它支持排期预期，不构成关闭依据 |
| Hw-23 | 作者报告现有权重在另一田块提线不佳，判断为小样本过拟合 | **口述经验；原因未经确认** | 第一版 A5 要求风险行写成"感知跨场地**不泛化（已知**，未量化）"。"已知"两字越界：机位、光照、生育期、采集条件都可能同时变了，不必研究，但不能排除 |

Hw-23 定稿措辞（替换第一版写法）：

> 作者报告现有权重在另一田块提线不佳，**原因尚未通过受控试验确认**。
> 本文不评价跨场地泛化；目标作业现场的可用性另行验证。

这一句同时保住三样东西：现场经验、研究边界、工程责任。

## 2. 改单 A · 主张与统计口径

### A1 — 场地限定语（决定 5）

第一版写"V1 主张限定为**与训练数据同场地条件下**"，同时又说田块不记录。
**这两句不能同时成立**：不记地块身份，就没有任何数据能证明测试图与训练图同田块。
改为按可追溯的采样范围描述，不补造逐图信息：

> 本研究的数据来自同一采样区域（含若干条相邻田块，未逐图建立地块身份标签）。
> 按连续采集批次划分训练/验证/测试集；不评价地块间差异，
> 既不作同田块泛化推断，也不作跨田块泛化推断。

"相邻几片之间差多少"这个问题在现有数据里无从回答——照实写，别让读者以为藏着。

### A2 — V1 两类量分开（决定 6），并把聚合定义清楚

"不按条件拆分"是**呈现方式**；"全集聚合"必须说是**哪个集**。第一版没定义，
是它被指出的真缺陷。定稿：

| 报什么 | 用哪个数据 | 为什么只能是它 |
|---|---|---|
| 横向误差：均值／标准差／95 分位／最大值 | 精选标注照片（人工独立标目标线） | 只有这里有**独立真值** |
| 失败率、最长连续失效 | 3 段连续视频（清单 §4 已列，≥30 s，明确不进训练集） | 只有这里**保留时间顺序**，且**未预筛**——无墙、遮挡、草密段都在内 |
| `status=dual` 双墙占比 | 两个集分开报，并注明照片集偏高 | 见下 |

四条必须写死的口径（不新增实验，属于评价有效性定义）：

1. **有符号还是绝对误差**：均值与 95 分位必须分别标明；不能用正负相抵的均值报精度。
2. **失败帧进分母**：误差统计只含"有独立真值且有有效预测"的帧；无法定义误差的帧
   记入失败率分母，**不得记 0，也不得悄悄剔除**。
3. **加权方式**：按帧还是按视频/批次等权，选一种写明（视频长度不等会改变聚合权重）。
4. **最长连续失效**只在保留时间顺序的独立视频上算，注明单位是帧还是秒及所用时基；
   **不得用散拍照片按文件名拼接失效时长**。

**分母悖论与它带来的采集侧后果（必须改采集清单）**：清单 §3 要求标注级照片
"画面内必须含两条完整芍药行墙"。在这类预筛样本上算出的失败率，含义只有
"**在两墙可见的精选样本中失败的比例**"。因此该集合**不负责**田间失败率——
这就是 A2 把失败率交给视频的原因。**副作用要如实说**：
照片集合上报告的双墙占比会**系统性偏高**，须与视频集合的占比并列。

**"dual 只代表算法说自己看到墙，不代表墙的位置对"** 这句也照写，
它由 A2 的分工自然消化：正确性由照片集真值负责，视频集只回答"能不能持续用"。

### A3 — 删掉两处第一版自己造出来的推论

- **"九格"推论撤销**：第一版称"真正对得上 240 的是光照×行况九格（240÷9≈27/格）"。
  撤销——原清单优先级是**非均衡**下限（主工况 ≥60、逆光中度 ≥30、阴天中度 ≥30、
  干净/茂密各 ≥15，名义和 150，且各项有重叠），既没列九格配额也不保证组合全覆盖，
  推不出九格。**第一版在撤回一个无依据推论之后，自己又写了一个同类错误。**
  改为：

  > 目标总量约 240 张，按既有优先级做非均衡覆盖，不要求四维全组合；
  > 视角为附带覆盖；**时段未记录，故不作"已覆盖"声明**。

  仍保留的一处订正：原句"四个维度全覆盖 + 每格 ≥15 张 + 合计 240 张"确实互斥
  （完整交叉为 3⁴=81 格），这句本身要改，但**不得**由此推出新配额。

- **"行业公认必须同田块"撤销**：第一版称台账 refs 第 92 行记的是"行业规矩：GOG
  必须用同作物同生长期同田块数据训练"，并据此说"换田块不保证是本领域公认属性"。
  审查方查了 OWL 现行官方文档：其训练建议是采集符合**具体现场条件**的图像
  （光照、土壤、生长期、作物），并允许使用适合条件的既有数据集——
  **不支持"必须同田块"这一概括**。我们那条本就是二手笔记里的概括，第一版又把它
  抬高成"公认属性"。删去"行业规矩／公认属性"这类话，**直接写本研究自己的数据与
  验证范围**，不借他人的未完成来给自己的范围选择背书。

- **李俊那条只保留它能支撑的部分**：核过原始 PDF 的是审查方——其展望确实自述
  "仅在理想天气/光照条件下做导航线提取""尚未进行沿提取导航线的自主行走试验"。
  它支撑的是**"同类工作会这样写局限"这一句式先例**（作者要求的"跟他们一致"落在这里），
  不支撑"本领域模型必须同田块训练"，也不支撑"本项目无需验证自主行走"。
  ⚠️ 本地 refs 记的页码 p73 与其核对结果（PDF 第 72 页／正文第 61 页）不一致，
  标为**待我方自查更正**，不照抄。

### A4 — v1.0 §8 假设行与 §9 风险行

- §8 第 231 行 `感知域差可修复（秋季微调后可用）` →
  改为 `现有权重在目标田的可用性：由 S0 判定（见 §4.2）；微调在其后，不作为秋季试跑前提`。
  **第一版没发现这行与 §7 排期构成硬矛盾**，见 §5。
- §9 第 243 行 R4 → `另一田块表现变差的原因未确认（Hw-23）；本文不评价跨场地泛化`，
  处置：按 A1 限定范围 + 按 §5 的 S0 判据决定秋季是 AUTO 还是遥控采集。

### A5 — 论文侧只登记，本批不动笔

开 **P15** 进 v1.0 §5 P 区清单：第 3 章 V1 小结与第 6 章展望需同步 A1/A2 的口径
（"同一采样区域内"、两类量分两个集、不主张跨场地泛化）。
执行走 `chucao_prj/thesis_cau/` 例外授权，**手术式提交、每条独立 commit 并注明 P15**；
**本批只登记不执行**，避免文档批里混进未审的论文改动。

## 3. 改单 B · 台账与文献笔记

- Hw-22：等级从"✅ 由作物学依据关闭"降为**作者基于当地经验的预期**，理由保留，
  并写明"不构成现场可见性的证明"。
- Hw-23：按 §1 定稿措辞改写（"原因尚未通过受控试验确认"）。
- **新增**：`docs/refs/参考资料调研.md` 第 92 行"必须用同作物同生长期同田块数据训练"
  加 **⚠️ 待核验** 标记并注明出处版本未记、与现行官方文档表述不一致。
  第一版的扫描范围漏掉了这个它直接引用的文件——**不改它，下一个人会从旧笔记
  把同一条推论重新引进来**。
- 李俊引文页码标注待自查更正（A3）。
- §4 修改记录表补两行（倒序最前）。

## 4. 改单 D · 门 N 验收：把软件约束与机械约束拆开（第一版混了）

第一版写的"抓线上帧 + 手推该轴不动双验收"混淆两件事：**撤使能后能否被外力推动，
取决于机构、制动与物理锁定，不能证明软件有没有发运动命令**。拆成两条分别验收：

- **软件约束**：在进入／退出空跑、旧命令残留、掉线恢复、急停复位四种场景下，
  **不产生被禁止的运动输出**（证据＝抓线上帧 + 固件输出状态）。
- **机械约束**：按实际采用的锁定方式确认刀梁与滑台状态（证据＝现场核对表 + 照片），
  判定标准（多大力、允许多大位移）**目前无依据**，本批不发明数字：登记进缺口清单 **B6"实物确认 5 项"类**（作者看一眼滑台与刀梁的锁止机构就能定），定为待补项而**非**待我想一个数。
- 双向都不许反推：不得仅凭"手推不动"判软件约束通过，
  也不得仅因"撤使能后可推动"就认定软件禁止运动失败。

**失视觉观测的措辞降级**（第一版夸了，zcode 上一轮连那句一起批了）：
`run_log.csv` 的 `frame_age_s / recv_timeouts / link_drops` 三列是**症状计数**，
只承诺区分"本地读超时／会话结束、无可用新帧、有图但感知不可用"三类现象；
**相机与网络的根因需车端记录佐证，无车端证据时标未知**。

门 N 该项验收要覆盖的四种场景（沿用已批的"不新增第四项"，仍属第一项的观测面）：
持续无帧；有帧但无可用墙；图像恢复；期间人工夺权。
每场分别核对：状态显示、发出的命令、MCU 输出、实际停止过程。阈值沿用经审查的
控制契约，不在本改单里另设数值。

## 5. 改单 C · S0 升回门 N 的可行性判据（决定 7；第一版降错了）

**为什么要升回来**：§7 把"标注、微调"排在 10 月中→11 月底，而门 N 自主沿行排在
10-05→10-15——**秋季试跑用不上微调后的模型**，只能用手上这套夏季权重；
而现有材料对它的评价正是"过分割 fg≈0.55、双墙不可用"（缺口清单 §C1、采集清单 §4 依据行）。
所以"现有权重能不能提线"不是工量参考，是**能不能跑 AUTO 的判据**。
第一版用"泛化不在研究范围"当作免拦的理由，属不相干推理。

定稿：

> S0 顺路收集可行性线索（不新增一趟），作门 N 试跑前的**输入有效性判据**：
> 现有权重提不出墙 → 秋季按既有退路降级为"遥控 + 感知在线记录 + C1 照采"，
> **不动搬运日、不等微调**；提得出来 → 按门 N 跑 AUTO。
> 进入受控试跑前另确认画面、标定与输入有效性，并验证失视觉时的实际停车行为。

**排期自洽性**：这么定之后，"试跑依赖微调"的暗含矛盾消失——因为试跑明确不依赖它。
§7 与 §8 相应两行同批改。**不得**一面在 §8 写"微调后可用"，一面把微调排在试跑之后。

S0 的产出仍**不含**按条件的分组结论（与决定 甲/2/3 一致）。

## 6. 改单 E · 一致性清理（第一版八处 + 本版新查出三处）

| # | 位置 | 改成 |
|---|---|---|
| E1 | README `--input data/autumn_data/raw` 会被自家嵌套拒绝挡住 | 改成一次一个条件目录；**布局契约待作者定** |
| E2 | 图的家一会儿 `zcode/autumn_data/raw/`、一会儿 `qoder/data/autumn_data/` | 钉成 qoder 工作区内一个 |
| E3 | 采集清单第 11 行"手持模拟 ~0.9 m"与第 10 行 100 cm 打架 | 手持模拟也按 1.0 m；俯角留给 M11b |
| E4 | "四个维度全覆盖／每格≥15／合计240" 三句互斥 | 按 A3 的非均衡表述；**不引入九格配额** |
| E5 | 分组键文档"视频/地块/日期"（布局里没有地块这一级） | 统一为"一段连续拍摄（批次）"；代码 docstring 与帮助文本同改，不动行为 |
| E6 | 缺口清单第 36 行仍印着已撤回的"60 cm 需压行行走" | 改撤回后口径 + 挂 M14/M15 |
| E7 | ① 板数字全旧（mjpeg 4→15；Code/警告；E2③④ 未上板） | 按 09-21 实测更新 |
| E8 | README 第 218 行 "IPM 接线属 E2③" | **E2⑤**（我写错的编号） |
| **E9** | **v1.0 第 33 行 V2 那格"还差什么"仍写 `B1/B2/B5 未过`** | **本版新查出的残留**：B5 又挂回秋季 V2 前置，与门 N／门 F 拆分（秋季不含 B5）直接矛盾。这是 09-21 R2 该关掉而没关干净的一处，第一版改单也没列它 |
| **E10** | v1.0 第 217 行"标注、V1 **分层**评价" | 改"V1 总体评价（照片集误差量 + 视频集失效量）"。第一版只列了 32/45/234 行，**漏了这行与 33 行**，审查方"只列出部分替换位置"的批评成立 |
| **E12** | 采集清单 §4 三段视频的用途行现在只写"微调后的留出验证（回放测 status=dual 比例）" | A2 把**失败率与最长连续失效**交给了这三段视频，用途行要照写；并注明"拍摄时不挑画面——无墙、遮挡、草密段都要留在里面"，否则这个集被预筛过，失败率就失去意义。**这不是新增采集**：素材与趟次不变 |
| **E11** | "同一件事在两份文档各说一遍就是缺陷"（第一版 §2 纪律） | 收敛为：**禁止的是多个可独立修改、会互相冲突的权威源**；README 摘要、论文自包含的方法描述、不可变历史记录可保留并标来源。主方案本就要能独立读 |

## 7. 明确不做

不加田块维度、不加采集记录表、不做 leave-one-field-out、不做 EXIF 时段反推、
不设"不同条件下效果对比"小节、不为远地探针集再跑一趟、不改两个工具的**行为**
（E5 只动 docstring 与帮助文本）、不改论文正文（P15 只登记）、不开 E3。

若日后要证泛化，最小增量仍是：30–50 张位置真分开的图 + 只标目标线 + manifest 里
一个独立 held-out 名字。**记在此是为了下次不必重新想，不是为了现在做。**

## 8. 验证方式

1. **反向 grep**，范围 = 现行文档集（历史 spec 与 ch02 旧措辞保留原样）：
   `需压行行走`、`压过 4~5 行`、`全部可跨车迁移`、`五条件一条都不看`、
   `告警并退回逐图`、`低机位`、`四个维度全覆盖`、`九格`、`行业公认`、`同场地条件下`、
   `手推该轴不动`、`E2③ 未完项`——**只允许出现在"撤回／订正／举例"上下文**。
   命中数为零本身**不构成通过**，须能解释为何为零（09-21 二批教训）。
2. **改号／改名类改动**：全仓枚举新旧两种写法并打印命中处，逐处判定是"规范条目"
   还是"对别处的引用"（后者最危险：不报错，只会变成假话）。
3. **表格列数一致**；`分层` 一词逐处眼看，确认第 27／269 行"按验证环节分层"未被误删。
4. **主张自检**：V1 每个量都能回答"在哪个数据上算、分母是谁、有符号还是绝对"；
   §2 存在"本文不主张"清单；R4 不再用"未验证"这类含糊说法；
   **Hw-22/Hw-23 的等级栏与正文主张强度一致**。
5. **范围核对不能只看测试数**：`pytest -q` 应为 **135 passed**，但同时必须核对
   ① 代码 diff 仅 docstring/帮助文本 ② 断言未增删 ③ 运行环境与被审版本一致。
   **测试通过或总数相同，都不验证文献原句、测量口径或因果结论。**
6. **排期自洽**：§7 与 §8 中不存在"秋季 AUTO 依赖微调"与"微调排在秋季试跑之后"并存。

## 9. 谁做、什么时候

qoder 单批完成（纯文档 + 两处代码注释），HEAD 报作者转 zcode。
**不占作者下田时间**；作者侧仍只有 v1.0 §4.2 那几项，其中 S0 已按 §5 升回判据，
它的结论**能替你省掉一次白搬车**——这是它比第一版值钱的地方。

# 缺口清单（v0.7 执行进度对照）— 2026-09-02

原则：**能写的代码已全部写完**；剩余项按"为什么卡住"分类。打勾即完成。

## A. 已完成（代码+测试）
- [x] `common/protocol.py` 帧协议（CRC16/收发/流式拆帧）+ 10 pytest
- [x] `pc/perception.py` 感知链（去畸变→分割→IPM→双墙，远/近场分离）
- [x] `pc/replay.py` avi 离线回放 + CSV 日志（训练域图验证 status=dual）
- [x] `pc/control.py` 中间刀 PID（抗饱和）+ 时延补偿 + 差速运动学 + 横向速率估计
- [x] `pc/state_machine.py` MANUAL/AUTO/LIFT/ESTOP（ESTOP 锁存）
- [x] `vehicle/bridge.py` UDP↔UART 桥接 + 500ms 看门狗（逻辑层有测试）
- [x] `vehicle/stream_pi.py` + `config.yaml` 图传管道
- [x] `firmware/protocol.c/h` + `firmware/retrofit.c/h`（SPL 风格，**未编译**）+ 集成说明
- [x] `common/uart_vectors.py` 8 组串口对拍向量
- [x] `docs/refs/参考资料调研.md` 两篇论文 + 4 个开源项目调研
- [x] pytest 全套 28/28 通过

## B. 缺：硬件在场才能做（用户侧）
1. **Keil 编译固件**：retrofit.c 未在 MDK 编译过，按 `firmware/集成说明.md` 6 步集成，报错发我修
2. **补 retrofit_get_wheel_cmd**（集成说明第 4 条的两行实现）并 patch `car_control.c` 的 Out_Pwm
3. **接线**：USART6 PC6/7→树莓派、刀继电器 PD4/5/6、步进 PB6/PB5/PB7、限位 PD15/PD10/PD11
4. **两个系数标定**：`STEPPER_STEPS_PER_MM`（百分表）、轮速 `c_per_pulse`（手转轮）
5. **AD5 电流读数**接入 `adc_current_a()`（iic.c ADS1115 已有驱动，换算系数标定）
6. **实物确认 5 项**（v0.7 §13.3）：PA5/PF10 继电器用途、NBREAK2、SBUS 映射、遥控器空闲开关、U7 是否让出 USART6
7. **树莓派采购/到货**后刷系统、装 GStreamer、跑 `vehicle/stream_pi.py`

## C. 缺：数据/标定（田间或实物在场）
1. **低机位 IPM 重标定**：当前 IPM_SRC 梯形是占位参数，需 0.9 m 机位 + 田面 40/50 cm 标记点重推（§5.6）
2. **秋季样本 200-300 张**：按采集清单（待我下发）拍，用于微调——avi 帧域差异已实测证实（过分割 fg≈0.55）
3. **avi 视频域验证不达标**：中心线在 avi 帧上不可用，属预期，微调后复测
4. SBUS 通道-速度映射表（固件加打印标定）
5. 时延实测数据（链路通后跑 LatencyCompensator 积累 p50/p95）

## D. 缺：我侧待写（下一批）
1. `pc/main.py` 主循环（串起 感知→控制→状态机→UDP 发送→CSV/GUI）——感知与控制件都已就绪，只差装配
2. `pc/gui.py` OpenCV 监控面板（分割叠加/中心线/模式/时延）
3. `vehicle/bridge.py` 生产 main() 的真机联调（逻辑已测，串口/套接字装配需树莓派）
4. 秋季采集清单 + 标注规范（W5 前下发）
5. 方法章理论材料：运动学误差模型、时延补偿推导、IPM 误差分析
6. 固件 C 代码的桌面单元测试（把 protocol.c 编译成 DLL 用 pytest 对拍 python 版——可选，台架对拍已覆盖）

## E. 外部依赖
1. **git push**：GitHub 连接中断中，本地 9 个 commit 待推
2. 弹齿刀采购到货
3. 导师对创新点表述的认可（v0.7 三点式）

# 缺口清单（详细执行版）— 2026-09-02，对照 v0.7 方案

> 用途：这是项目的唯一"欠账台账"。每项给出：**做什么 / 为什么还卡着 / 具体怎么做 / 验收标准 / 预计耗时 / 依赖**。
> 完成一项就在编号前打 ✅ 并写日期。每周五对照本表过一遍（对应排期周里程碑）。
> 状态标记：🟢 已完成 ｜ 🔴 未开始 ｜ 🟡 进行中 ｜ ⚪ 可选/暂缓

---

## 📊 总览：做完的 / 没做的（2026-09-21 快照，qoder HEAD `2e2ee40`）

> **口径**（AGENTS.md 审查清单第 5 条）：已设计 ≠ **已编码** ≠ **已台架** ≠ **已田间**，逐态分列。
> "89 条测试通过""Keil 0 错误"只到 **已编码+已编译**，**不构成台架或田间证据**。
>
> **总判定：本表所有安全与控制权限类改动，状态停在「已编码 + 已编译」，0 项达到「已台架」。**
> ⇒ 两条硬禁令**继续生效**：① 禁止实车 AUTO；② 禁止按现有接线文档直接带动力接线。解除条件仍是 **B5 安全动作实测全项通过**。

### ① 🟢 已做完（代码 + 编译/测试留证，不需要再动，只等硬件复验）

| 项 | 做到哪一态 | 证据（可复核） | 还差什么才算"完" |
| --- | --- | --- | --- |
| A1–A12 全部代码侧交付 | 已编码 | 当时（09-14 收工）pytest **89 passed**；Keil 0 错误。**当前基线看下面 E1/E2 行，不沿用旧数** | 见 ②，台架/田间复验 |
| H1.1 / H1.2 数据资产护城 | 已编码 | `tests/test_data_tools.py`（5+4 例） | — |
| H1.4 训练权重输出路径 | 已编码 | `train_m.py` `SAVE_WEIGHT_DIR` 指回工作区 | 真跑一次训练确认落盘位置 |
| H1.5 混写拒绝 | 已编码 | 复现评审 7/4/3 场景 2 例 | — |
| **H1.3 / H1.6 分组划分 + 清单冻结机制** | **已编码，未冻结** | `split_dataset.py` 分组模式 + `split_manifest.json`（qoder `9e5774c`）；**09-21 E1② 补**：硬性保证 train/val/test 各≥1 组（旧写法恰 3 组时 test 为 0），组数 <3 改为**报错拒绝**而非静默退回逐图 | **等 C1 秋采数据**才能填分组清单；机制建好 ≠ 泄漏已证伪 |
| H2.1 / H2.5 / H2.6 权限与桥接 | 已编码 | `state_machine.py` 契约 + 4 测试；`bridge.py` `last_real`/`last_inject` 拆分 + 跨边界 4 例 | ② 的 ①②⑥ 三条 |
| H2.2 / H2.3 / H2.4 固件侧 | **已编码 + 已编译** | **09-14 命令行 `UV4 -r`：0 Error / 61 Warning（同遗留基线）/ Code=44840 / hex 已再生 / `PWM.map` 内 `retrofit_sbus_ok@0x08008863`**；`build_log.txt` 已更新 | 烧录 + ② 的 ③④⑤⑦ |
| H2.7 前轮 Ackermann 几何 | 已编码 | `tests/test_nav_geometry.py` 5 例（直行极限/后轮零角/共 ICC/刚体速度/左右镜像） | 台架转角对拍 |
| H2.8 STATUS 新鲜度/序号（**前半**） | 已编码 | `status_rx.py` `STALE_S=1s`、u16 序号新旧、`out_of_order`，+3 测试；**09-21 E1① 补**：MCU 重启后序号回退的**重新同步**（判据＝连续 ≥50 帧序号互不相同且递增且已判 stale；重同步只恢复状态显示，**不恢复 AUTO、不清急停锁存**），+4 测试 | **后半未做，见 ③-B** |
| **E1 两个自写 bug（09-21）** | 已编码 | `status_rx.py` MCU 重启重新同步（判据＝连续 ≥50 帧序号互不相同且递增且已判 stale；重同步只恢复状态显示，不恢复 AUTO、不清急停锁存）+4 测试；`split_dataset.py` 三集合各≥1 组 + <3 组拒绝 +3 测试。全量 `python -B -m pytest tests/ -q` 09-21 收工 = **135 passed**（09-21 复审基线 117 → 撤回一条"跨行即压苗"的过度断言使几何判据 8→7，预标注人工成果保护 +2，IPM 生产者—加载器闭环与数值探针 +2） | 待 zcode 审；**测试数上涨只说明编码层无回归，不改变"未台架"的总判定** |
| **R3 整机空间口径** | 🟡 **车宽已定，田块横断面未定** | 09-21 已把"量车"这一半关掉：`ch02:14` 的 ≤60 cm **是硬错误**（样机 2.15 m / 上代 3.25 m，Hw-20 现场确认），所以"60 cm 放不下 3 把刀"这个两难本身不成立，不用再纠结。**剩下的阻塞换了问题**：轮迹相对作物行的横向位置、胎宽、作物保护带、行驶跟踪误差四项全部未测（M14/M15 + N1），伤苗率分母口径因此未定。**⚠️ 09-21 复审撤回了本行旧写法**："3 条带需 ≥95 cm 车宽、60 cm 车必须压在作物行上行驶"——"跨几个行距"推不出"压几行苗"（行距 38.2 cm 时 191 cm 恰为 5 个行距，两轮正落在行间）。`tools/geometry_check.py` 已改为只报事实，量表见 `docs/整车尺寸实测表_20260921.md`（M3、M7–M15） | **门 N（导航验证准入）第一步**：用户到田里拍横断面 + 量 M14/M15 → 定"一次几条带"与伤苗口径 → 再改 ch02/deck/V4 布样 |
| H3.1 / H3.2 live 链路与配置 | 已编码 | `common/mjpeg.py`（4 测试）+ `stream_pi.py` TCP multipart + `config.yaml` ttyS1 | 上车实测（B7） |
| H4.1 / H4.2 / H4.3 / H4.5 论文口径 | 已改入文稿 | chucao_prj `be9093a` 三条手术提交 | 送审前整章通读 |
| H5.5 `train_m.py` NameError | 已编码 | test 分支改 `t_*` 计数 | — |
| H2.9 时延验收口径 | 已改文档 | X5 清单 + v0.9 §7 四项分列 | 实测四项数值 |
| **空间口径判据 + 预标注入口（09-21）** | 已编码 | `tools/geometry_check.py` +8 测试（文档口径下 60 cm 车只放得下 2 把刀、且需压行行走）；`tools/pre_annotate.py` + `tools/启动预标注.bat` +4 测试 —— 实测 `deeplab` conda env 与 anac 基解释器都能跑，`labelme_env` 缺 cv2/torch | 量车结果决定 ch02 与 deck 怎么改 |
| **本轮新增：SBUS 心跳按帧有效性门控** | 已编码 + 已编译 | `telecontrol.c:161-171`（`ef7d905`）；旧写法浮空线乱码会喂狗，失联保护只覆盖一半故障 | ② 的 ④(b) |

### ② 🔴 已编码但**必须上台架才算完**——复验清单（全部用户侧，我代替不了）

| 编号 | 动作 | 通过标准 |
| --- | --- | --- |
| B1 | 烧录 `OBJ/PWM.hex` | 能启动、MANUAL 遥控正常 |
| B2 | 串口协议对拍 + H2 组合场景 | ① MANUAL+失视觉→线上只有 HEARTBEAT；② AUTO+失视觉→NAV(0,0)+TOOL 提刀；③ 横移中切 AUTO→四轮角无 90° 残留；④ 断 SBUS **(a) 关接收机 (b) 线断悬空出乱码 两子情形都要过** ≤500ms 四轮停；⑤ SBUS 恢复→可继续手动；⑥ 连续 2h 桥接无重复注入 |
| B3 | 接线（含继电器板照片定刀控） | P0-7 闭环 |
| B4 | 三系数标定（Fw-1/2/3） | 百分表/卷尺反算 |
| B5 | 安全动作实测 | **全项通过才解除两条硬禁令** |
| B6 | 实物确认 5 项 | 看一眼就有答案 |
| B7 | RDK X5 上车 | 掉线/供电/USB 短线 |
| B8 | 链路一票否决 | 控制帧往返 p95（**不是** main.py 感知 latency 列） |
| Hw-17 | 台架打印 `rec_sbus_data[33]` 三态值 | 正常 / 拔接收机 / 遥控器失联 |
| ⑦已知遗留 | — | MANUAL 失联只清 `veloc`，`send_angle` 仍保持最后转角：按"可接受"记录，**不许当作失联已完全仲裁** |

### ③ 🔴 明确**没做**（按谁能推动分三类，不含任何"部分完成"粉饰）

**A. 只能你（用户）做 —— 要人或实物，不是代码问题**
- **【S1，本周】把控制栈装上上代车（3.25 m）跑通链路**：B1 烧录 → B2 对拍（含 SBUS 失联两子情形）→ 电脑能否控住、失联能否停车（台账 Hw-21、v1.0 §4.2）。这些**软件模块与调试记录可复用**，必须在搬运前做完；但"复用"不等于"搬完不用测"——接线、执行方向、失联是否真停车、急停切断范围、供电与实测时延，到样机上要**逐台再确认**（v1.0 §4.1）
- **【S3 是一次性事件】搬到真实车（样机，在地里、不好运）前**：先断电逐项核对接线（48V 极性、共地、保险丝、刀梁锁定、急停切断范围），带备件（USB3.0 短线、保险丝端子、网线、便携电源、卷尺与地面标记签）
- **【门 N 现场项，只能在真实车上做】补齐实测 + 样机机位标定**（量表 `docs/整车尺寸实测表_20260921.md`，判据 `tools/geometry_check.py`，**该工具现在只报事实不报结论**）：M3 行距、M7 三把刀刃宽、M8 滑台实测行程（**两端合计**）、M9/M10 提刀传动比与离地量、M11b 相机俯角、M12 相机纵向位置、M13 数刀头、**M14 胎面接地宽度、M15 轮迹相对行线的横向偏差（09-21 复审新增）**；`ch02:14` 的"整机宽 ≤60 cm"已确认**是硬错误**（实际 2.15 m），不用再量
- **【N1 未决】田块横断面（09-21 复审后换了问题）**：轮距 191 cm ÷ 行距 30~40 cm = 4.8~6.4 个行距，这只是"两轮之间跨了几行"的算术，**推不出"轮下压过 4~5 行作物"**（本行旧写法已撤回；反例：行距 38.2 cm 时 191 cm 恰为 5 个行距，两轮正落在行间）。要判的是四件**实测**：① 轮迹相对行线的横向偏差（M15）② 胎面接地宽度（M14）③ 作物保护带 ④ 行驶跟踪误差；外加说明"行距 30~40 cm"指作物行间距还是垄间距。**在量出来之前，文档与论文两侧都不许写"压苗"或"不压苗"** —— 这决定 V4 伤苗率的分母口径
- C1 秋季图像采集（**10 月中前，全项目时间最紧的一项**，现在就能拍；预标注入口 `tools/启动预标注.bat` 已可用，且默认不覆盖已有人工标注）。
  **拍之前先定文件命名：`{批次}_{日期}_{序号}.jpg`**，一组 = 一段连续拍摄。09-21 查清采集清单原写的 `IMG_0001.jpg` 会让每张的分组键都变成 "IMG" → 240 张塌成 1 组、独立测试集不存在；现在 `split_dataset`/`pre_annotate` 都会拦住并说明真因，但**已经在田里按旧命名拍的素材要补改名**。详见 `docs/秋季图像采集清单.md` §6
- C2 标注与微调、C3 **样机机位外参（IPM）标定**（相机离地 100 cm，"低机位"是 ch02:23 的错措辞，见 Hw-20）、C4 SBUS 通道-速度映射表、C5 时延实测
- Hw-14 整车几何尺寸实测；Hw-11 伺服寄存器表核对；Hw-12 滑台接线
- H4.7 只读仓 `chucao_prj` 脏树清理
- ② 整表

**B. 我能写代码，但被前置卡住 / 需要你点头开批**
- **H2.8 后半**：PC 端 AUTO 许可叠加"MCU 状态新鲜度 + 急停锁存"——属状态转换契约，动了就是改控制权限语义，按分工要先经 zcode 审 + 你批准
- **H5.2** IPM 仍用占位几何，没加载已存在的 `ipm_params.json`
- **H5.3** 采集时间戳/帧龄拒绝（`rx_good/bad` 不能当丢包分母）
- **H5.4** 刀控/电流(返回 0)/电池(固定 48V) 闭环 —— 等继电器板（P0-7）
- **H1.7** 分组清单冻结 —— 机制已有，等内容物（等 C1）
- **NAV2 / 4WS 帧** —— 前置门是 Hw-11 轮角反馈是否真可用，未到已编码
- **H3.3** eth0 换链路对端偶发挂死 —— 未查，怀疑同卡双网段
- **H4.4** `data_manifest.md` 绑定运行 ID —— 等 C1/C2 产物
- **H4.6** 方法章理论：二阶式(4-6) 已勘误入文档；**09-21 实测确认 `DifferentialDrive()` 默认 `k_d=0.0` 且主入口未传该增益**（`control.py:86` 注释自认 “P-only until k_d set”）→ 组会 deck 第 12 页“轨迹跟踪用 PD 加横向速率阻尼”属过度声明，ė 只经时延补偿 `e+ėτ` 进入；论文侧不得继承该措辞，阻尼要么台架整定要么改述

**C. 等对侧（zcode）**
- 待 merge 进 GitHub 主线：**领先提交数以 `git rev-list --count zcode/master..HEAD` 当场实测为准**（09-21 已攒到两位数）。关键批次：`9e5774c` 分组划分、`ef7d905` SBUS 心跳门控、`8153c5e` 状态板、`1e529ff` B8 预测量、`189ee78`＋本轮 v1.0 方案、`ba0f68c` E1 修复、`2e2ee40` 预标注入口
- 主线合完后 qoder 侧 `git fetch zcode && git merge`，再开新批

### ④ 本轮（09-21）**没碰**什么，避免误解为已处理
- **E2 进度（09-21 复审批次后）**：**E2⑤（把 IPM 标定接进感知入口）做了一半**——标定器 `calib_ipm.solve()` 现在写来源字段，加载器 `pc/ipm_io.py` 已建并跑通"真实 solve() → 真实加载"的闭环；但 `perception.py` **仍用占位几何，`load_ipm_params` 没有调用方**，所以 IPM 链路**没有闭合**，剩下"加载器 → 感知入口 → 控制量纲"这一环。**E2③（SBUS `UART1_Rec_Len == SBUS_DATA_LEN(35)` 纳入有效帧判据）→ ✅ 09-21 已编码 + 已编译**：`telecontrol.c:142` 现在三条件与判（长度＋帧头 0x0F＋XOR）。命令行 `UV4 -r` = **0 Error / 60 Warning**（基线 61，少的正是"UART1_Rec_Len 算了但没用"，它现在被用上了），Code=44848（原 44840），hex+map 已再生。**口径：已编码＋已编译，不等于已台架**——B2 ④(a)(b) 两子情形（浮空线/断线不得刷新心跳、短帧不得更新通道值）仍待做。**E2④（`common/mjpeg.py` 有界读＋帧龄）→ ✅ 09-21 已编码 + 已单测**：阻塞式生成器 `iter_frames` 删除（调用即 `NotImplementedError`，防止有人顺手接回去），换成 `StreamFeed`：读线程自己扛 socket 超时与重连，控制循环只调非阻塞的 `get(max_age_s)`。`pc/main.py` 每拍要么拿到够新的图、要么拿到明确的 None（不再拿最后一帧重复推理，那会让"冻住的墙"看起来像健康的线）；`VISION_LOSS_S` 与 GUI 按键在流断时才真正可达。新增 11 条测试（无 socket 无线程地驱动 `_pump`/`_run`），其中一条用"connect 卡 5 秒"证明消费端 200 次轮询 <0.5 s 全部返回。**口径：已编码＋已单测，未与真机 X5 联调**。⚠️ 编号勘误：09-21 有一条 commit 把加载器标成 E2③，按 v1.0 §6 实际是 **E2⑤**，③ 是 SBUS 长度条件
- **E3 未动**：AUTO 许可叠加 MCU 新鲜度——动它＝改控制权限语义，须你批准 + zcode 审
- **P 区一行未改**：论文正文与 Word 仍是 `be9093a` + v14.docx；v1.0 §5 那 14 条是清单不是已完成
- **台架 0 项**：本轮全是文档与代码层，没有任何一项从「已编码」升到「已台架」
- 组会 deck 工作树的改动是**汇报人自己的**，我未覆盖；下次动它先重读
- `PWM.uvprojx:40` 那个孤立 `
` 仍未手工改（Keil 存盘会覆盖）；`PWM.uvguix.22763` 故意留脏不提交；`.gitattributes` 未加，等你定

---

## A. 已完成 ✅（代码侧，全部有测试或编译验证）

| # | 交付物 | 验证方式 |
| --- | --- | --- |
| A1 | `common/protocol.py` 帧协议（CRC16-CCITT-FALSE / 收发 / 流式拆帧 `extract_one`） | pytest 10 例，标准向量 0x29B1 |
| A2 | `pc/perception.py` 感知链：懒加载标定→去畸变→DeepLabV3+→IPM→双墙→远/近场中心线 | 训练域测试图 status=dual，中心线贴合 |
| A3 | `pc/replay.py` 离线回放框架 + CSV 日志 + 叠加图 | 训练域图回放通过；日志字段齐全 |
| A4 | `pc/control.py`：中间刀 PID（条件积分抗饱和）、时延补偿（p50/p95）、差速运动学 vL/vR、横向速率估计 | pytest 8 例，阶跃 300ms 稳态 ≤1mm |
| A5 | `pc/state_machine.py` 四状态安全机，ESTOP 锁存仅物理复位可解 | pytest 4 例 |
| A6 | `vehicle/bridge.py` UDP↔UART 桥 + 500ms 看门狗（逻辑与 I/O 分离） | pytest 4 例（看门狗时序边界） |
| A7 | `vehicle/stream_pi.py` + `config.yaml`（720p25 H.264/RTP 管道） | 代码审查（无 Pi 无法运行验证） |
| A8 | `firmware/protocol.c/h` + `retrofit.c/h`（UART6 链路/双指令源/刀控联锁/500ms 看门狗/TIM4 步进/TIM3 轮速捕获/20Hz 状态帧） | **Keil 0 错误编译通过，Code=44192 B** |
| A9 | 固件完整集成：main.c 挂 retrofit_init；TIM6 1ms 时基；telecontrol.c SBUS 心跳；car_control.c Out_Pwm AUTO 注入；旧 USART6 调试口让位 | 同上，hex 已生成 |
| A10 | `common/uart_vectors.py` 8 组对拍向量（含 2 组必须拒绝的坏帧） | 本机运行输出核对 |
| A11 | `docs/refs/参考资料调研.md`（王硕博士论文 + 李俊硕士论文 + 4 个开源项目，含章节页码级引用建议） | 文档在库 |
| A12 | 调试实证 4 条（可写进论文）：抗饱和实现不当→积分器卡死；执行滞后+原始微分→振荡；去畸变先缩放后 remap→四分之一画面 bug；zip 旧 OBJ→假链接冲突 | 全部有复现过程 |

---

## B. 🔴 硬件在场才能做（全部用户侧，顺序执行）

### B1. 烧录固件 🟢 **已解锁**（09-14 编译门槛 0 错误达成，等你接板子）
- **做什么**：把新生成的 hex 烧进大板（J-Link/ST-Link 或串口 ISP）。
- **hex 实际位置（09-14 实测核对，非 `USER/OBJ/`）**：`firmware/大车proj/大车/OBJ/PWM.hex`（130,641 B，随本轮 `ef7d905` 重生成；axf 同目录）。
- **怎么做**：Keil 里 `Flash → Download`，或 STM32CubeProgrammer 烧上面那个 `PWM.hex`。
- **验收**：上电后行为与烧录前完全一致（底盘遥控可开、刀继电器不动）——retrofit 只初始化不干预，MANUAL 是默认态。**注意本轮起 MANUAL 多了 SBUS 失联保护：烧完后若摇杆完全不动，先确认接收机已通电且能收到有效帧（⑤ 恢复可控），再判断是否接线问题。**
- **耗时**：10 分钟。**依赖**：无（编译依赖已满足）。

### B2. 串口协议对拍 🔴（固件bring-up 第一关）
- **做什么**：USB-TTL 接 STM32 的 USART6（板 PC6=TX→TTL RX，PC7=RX→TTL TX，共地），PC 发 8 组向量验证解析。
- **怎么做**：`python common/uart_vectors.py COM口`（自动循环发）或 `--dump` 手动灌。USB-TTL 若未采购见 E2。
- **验收**：① 6 组有效帧被正确执行（用心跳+导航帧即可观察：发 nav_straight 时若强制 AUTO 才动作，可先只验证"收到不崩+状态帧 20Hz 出现"）；② `bad_crc_must_reject` 和 `truncated_must_reject` 被静默丢弃（无任何异常动作）；③ PC 端能收到 0x10 状态帧（可用 `python -c` + pyserial 抓打印验证 CRC 通过）。
- **耗时**：1-2 小时（含接线）。**依赖**：B1、USB-TTL×2。

### B3. 接线（按 v0.7 §13.2 引脚表）🔴
| 连接 | 引脚 | 注意 |
| --- | --- | --- |
| USART6 → 树莓派 UART | PC6/PC7 ↔ Pi GPIO14/15，**共地**，Pi 侧 3.3V 电平直连（PC6 输出 3.3V，无需转换） | 树莓派 UART 默认给蓝牙，需在 config.txt 分配（见 B7） |
| 刀继电器 ×3 | PD4/PD5/PD6 → 外接继电器模块输入（74HC245 已升 5V，高有效） | 继电器模块建议光耦型，独立供电 |
| 中间刀步进 | PB6=脉冲 PB5=方向 PB7=使能 → 驱动器对应输入 | 共阳/共阴接法按驱动器手册；驱动器细分数记录下来算系数 |
| 限位开关 ×3 | PD15(原点)/PD10(左)/PD11(右)，开关接地，内部上拉，闭合=低 | 常闭型优先（断线即触发） |
| 急停回路 | 确认 PA5/PF10 现有接线（B6 ①②）后决定是否串入 | 保持常闭硬线原则 |
- **验收**：万用表逐点量通断；上电后限位开关状态能从 0x10 帧的 limits 字节读出（触发位翻转）。
- **耗时**：半天。**依赖**：B1；树莓派部分依赖 B7。

### B4. 三个系数标定 🔴（台架）
| 系数 | 位置 | 做法 | 验收 |
| --- | --- | --- | --- |
| `STEPPER_STEPS_PER_MM` | retrofit.c:15，当前 25.0 | 滑轨装好后：发 tool 帧 ±20mm，百分表量实际位移，反算；驱动器细分数改变时重标 | 10 次往返重复定位 ≤0.5mm（v0.4 §5.4） |
| `c_per_pulse`（轮速） | retrofit.c wheel_speed_update，当前 0.0005 | 顶起车轮，手转整 N 圈，对比 PC 收到的速度积分路程；或拖行 10m 实测 | 误差 ≤5% |
| AD5 电流换算 | retrofit.c `adc_current_a()`（当前返回 0 占位） | 万用表串入电机回路对照 AD5 读数线性拟合；把读取代码接到 iic.c 的 ADS1115 U18 通道 | 误差 ≤0.2A |
| SLEW_ACC 斜坡斜率 | retrofit.c:15，当前 0.2 m/s² | 阶跃切 AUTO 观察：斜率过大→仍窜车；过小→响应滞后（横向纠偏超调）。从 0.1/0.2/0.4 三档试，选定后记入论文标定表 | 切换瞬间轮速无可见跳变且纠偏超调 ≤20% |
- **做法细节**：`adc_current_a` 接线参考 `iic.c` 中 U18（I2C2，PB10/PB11）已有的读法，读 AD5 通道原始码 → 电压 → 按 ACS712-05B 灵敏度 185mV/A 换算，减去静态偏置。
- **耗时**：各 1-2 小时。**依赖**：B2（链路）、B3（机械件）。

### B5. 安全动作实测 🔴（整车台架，对应 v0.4 §8.2）
- **做什么**：逐项验证安全清单。怎么做与验收：
  1. **心跳超时**：进入 AUTO（遥控+PC 同时在线）→ 拔 USART6 线 → 车必须 500ms 内停 + 提刀。 stopwatch 实测（手机录像 240fps 数帧）。
  2. **坏帧注入**：发 `bad_crc` 帧 → 无任何动作。
  3. **ESTOP 锁存**：发 estop 帧 → 之后任何 nav/tool 帧无效 → 按物理复位（接 PD 上按键调 `retrofit_clear_estop`，或重上电）才恢复。
  4. **硬件急停**：按板上急停钮 → 驱动断电 ≤50ms（录像数帧）。
  5. **限位触发**：手挡中间刀滑轨限位 → 0x10 帧 limits 位翻转 + 步进停。
- **验收**：全部留视频/照片存 `hardware_ref/log/`（论文安全章节证据）。
- **耗时**：半天。**依赖**：B2、B3、B4。

### B6. 实物确认 5 项 🔴（看一眼就有答案，尽快做）
1. PA5 大继电器（SLA-05VDC）实车接的是什么？→ 决定硬件急停切哪路电
2. PF10 主继电器线圈回路的 NBREAK2 端子现接什么？→ 是否急停串入点
3. SBUS 各通道→四轮速度映射：固件加打印或看 led.c 里 switch_ch 用法，列成表发我（AUTO 指令量纲对齐用）
4. 遥控器有无空闲两段开关 → AUTO/MANUAL 切换判据（没有就用通道组合）
5. 485 页 U7（PC6/PC7 的 MAX13487）是否不贴/无外设 → 若占用需换链路串口方案（备选 PA9 单向不成立，会改设计）
- **耗时**：1 小时拍照+记录。**依赖**：无，**本周就做**。

### B7. 树莓派上车 🔴 → **已改由 RDK X5 执行**；✅ 09-10 X5 单板验机四项全过（烧录/SSH/UART ttyS1/推流 TCP-MJPEG 105ms@30fps，见 docs/X5验机与接入执行清单.md）。剩余：上车接 STM32 + 网桥换网线重测时延
- **做什么**：~~Pi 4B/5 刷 Bookworm~~ → **X5 烧 Ubuntu 22.04（Rufus）→ 网线直连 SSH（eth0 静态 192.168.127.10，PC 设 192.168.127.100）→ UART 用 40pin Pin8/10（UART1，3.3V，/dev/ttyS*）**。完整步骤/命令/验收见 **`docs/X5验机与接入执行清单.md`**（含 E2.2 到货当天四项验机：烧系统/SSH/推流/UART）。
- 车端配置差异：`vehicle/config.yaml` 的 `serial_dev` 用 `/dev/ttyS1`（以板上 `ls /dev/ttyS*` 实测为准），其余（stream_pi.py、bridge.py）不变。
- **验收**：PC 屏上出实时 720p 图，端到端时延（拍秒表对比法）<100ms。
- **耗时**：半天。**依赖**：E2 ✅ 已到（X5、USB-TTL×2、网桥）。

### B8. 链路一票否决测试 🔴（v0.4 §6.1 硬门槛）
- **做什么**：真实田间距离 120m：PC 开 5GHz 热点，Pi 连接，ping 100 包 + 控制帧打流 10 分钟。
- **验收**：丢包 <1%，控制帧 p95 时延 <50ms。**不达标 → 装 E4 的定向天线重测，直至达标才准进 M2**。
- **耗时**：1 小时。**依赖**：B7。

#### B8-预测量（2026-09-15，台架近距离，网桥在链路中）——**这不是 B8 验收**
- 拓扑：PC 以太网 192.168.127.100 →〔PC 端 CPE ←5GHz→ 车端 CPE〕→ X5 192.168.127.10。
- **X5 可达**：SSH(22) connect 3.0 ms 有应答；主机密钥为**新添加**（说明这台 X5 与 09-10 验机时的 host key 不同，可能重烧过系统，下次登录留意）。
- **网络 RTT 实测**（H2.9 四项里的**只有这一项**，其余三项未测）：
  - 32 B 载荷 ×30：**0 丢包**，1–2 ms
  - 1472 B 满包 ×100：**0 丢包 / 0 挂死**，min 1 / median 4 / mean 6.5 / **p95 17** / max 21 ms，stdev 5.6
- **观察**：32 B 载荷 1–2 ms，1472 B 满包 p95 17 ms、14–17 ms 长尾 35/100。**推断（未证实）**：时延随包长增长符合共享无线介质特征，故"网桥在路径上"的可能性大；**但这不是证明**——要确认拓扑需查现场接线、路由表与两端设备状态（09-21 复审指出我把这句写得比证据强）。
- **观察**：TCP 22 有应答、5000 连接**超时**。**推断（未证实）**：X5 上 `stream_pi.py` 未启动；注意"没人监听"通常立刻返回 refused，超时更像被丢弃/防火墙或主机未响应，故**不能据此断定推流进程状态**（09-21 复审指出）。因此**视频同框时延这项今天没测到**。
- **H3.3 未复现**：本轮无"换链路后对端挂死"；但该故障需要**切换链路**才触发，纯 ping 复现不了，仍列为未查。
- **B8 仍然算未做**，缺的是：120 m 实距、控制帧打流 10 min、以及 p95 <50ms 的**控制帧往返**口径（满包 ICMP 不能替代）。

---

## C. 🔴 数据/标定（算法质量类，多数可并行启动）

### C1. 秋季图像采集（**时间最紧，10 月中前必须完成**）
- **做什么**：按采集清单（D4，我下周下发）在田间拍 200-300 张 + 3 段新视频。
- **为什么紧**：avi 帧域差异已实测证实（过分割 fg≈0.55，双墙提不出）——现有权重在当前田间图像上不可用，不补数据 M2 的"感知回放通过"里程碑必跳票。
- **做法**：等 D4 清单（内容：机位/角度/光照矩阵、分辨率、命名规范、每类最少张数）。
- **验收**：清单覆盖率 100%，图像入 `D:\ai work\zcode\autumn_data\raw\`。
- **依赖**：无（现在就能拍，趁草还绿着）。

### C2. 标注与微调 🔴
- **做什么**：labelme/多边形标注（我出规范）→ 与夏季数据混合 → `DeepLabV3Plus-Pytorch-master` 微调 best_model.pth（可上 Colab 免费卡）→ avi 留出段 + 新视频回放复测。
- **验收**：新视频回放 status=dual 比例 ≥80%，中心线肉眼贴合；mIoU ≥0.75。
- **耗时**：标注 2-3 天 + 训练 1 天。**依赖**：C1。

### C3. 低机位 IPM 重标定 🔴
- **做什么**：相机装上 0.9m 机位后，田面/水泥地摆 40/50cm 标记点 → 拍图 → 重推 IPM_SRC 梯形与 m/px 比例 → 改 `pc/perception.py` 的 IPM_SRC/IPM_SCALE_X/Y。
- **做法**：棋盘格内参沿用 calib_params.npz；IPM 用地面 4 点单应即可（我会写一个 `pc/calib_ipm.py` 一键算）。
- **验收**：3 个标记点投影误差 ≤2cm（v0.4 §5.6）。
- **耗时**：1 小时拍摄 + 我 2 小时计算。**依赖**：相机支架装好。

### C4. SBUS 通道-速度映射表 🔴
- **做什么**：B6③ 的表 → 我把 `retrofit.c` 的量纲换算核对/改写。
- **验收**：AUTO 模式下发 vL=vR=0.14，车实际走 0.5km/h（GPS 或秒表量 10m）。
- **依赖**：B6、B2。

### C5. 时延实测 ⚪（链路通后自动积累）
- **做什么**：LatencyCompensator 已内置 p50/p95 统计；链路通后跑 10 分钟积累，写入 `results/latency_log.csv`。
- **验收**：p95 <50ms（与 B8 一致性检查）。
- **依赖**：B8。

### C6. ⚪ 错误帧回归集（可选）
- 把台架发现的任何异常帧存成回归用例，加入 `tests/test_protocol.py`。
- ✅ 09-05 协议健壮性核实结论（全部有测试覆盖或核实无风险，无需改动）：① `extract_one` 对长度非法/CRC 失败均逐字节重同步（`tests/test_control.py` corrupt 用例覆盖）；② 最大帧 521 字节，bridge `recvfrom(2048)` 余量充足；③ seq 掩码是调用方纪律（pc/main.py、bridge 均已 `& 0xFFFF`；pack_frame 超范围直接 raise，保持 fail-fast）；④ `unpack_status` 等定长校验抛 ValueError——接收侧实现时必须接住，见 D7。

---

## G. v0.7 方案旧路径勘误（2026-09-05，工作区整合后）

> v0.7 docx 交付矩阵写于整合前，以下路径已迁移；**资产位置以根目录 README.md 为权威**，v0.7 相应章节按此对照。下次出 v0.8 时一并修正。

| v0.7 写的旧路径 | 现位置 |
| --- | --- |
| `prj_xiangjibiaodi/相机标定工程/calib_params.npz` | `data/calib_params.npz`（加载方：`network/camera_calib.py`、`pc/perception.py`） |
| `avi_project/3 段田间作业视频 field_video1~3.avi` | `data/videos/`（已 gitignore，仅本地） |
| `avi_project/295 帧抽帧 frames1` | `avi_frames/`（已 gitignore） |
| 根目录 `morph_process.py`（peony_postprocess / extract_dual_walls 等） | `tools/morph_process.py`（pc/perception.py、network/local_single_station.py 引用，两侧 sys.path 已加 tools/） |
| `chucao_prj/model_data/` 数据集/权重 | `model_data/weights/best_model.pth`（本仓库内，路径未变） |

另：v0.7 中 bridge "naive stream split" 描述已过时——串口跨读边界丢帧缺陷已修复（abde261，`poll_serial_frames` remainder 缓存 + 4 个重组测试，34 例全过）。

---

## D. 🔴 我侧待写（下一批交付，均已排好）

| # | 交付物 | 内容 | 预计 |
| --- | --- | --- | --- |
| D1 | ~~`pc/main.py` 主循环~~ ✅ 09-02 | 回放/实车双模式，含 ESTOP 横向超限联锁（实测 -0.12m 触发）、心跳维持、CSV；3 帧冒烟通过 | — |
| D2 | ~~监控面板~~ ✅ 09-02 | 并入 main.py：分割叠加+HUD+按键 [A]uto/[M]anual/[E]stop/[R]eset/[Q]uit | — |
| D3 | ~~秋季采集清单~~ ✅ 09-02 | ：拍摄矩阵/内容要求/命名规范/标注规范 | — |
| D4 | ~~`pc/calib_ipm.py`~~ ✅ 09-03 | grab/pick/solve 三步流程，合成数据自测：0.8px 噪声下重投影误差 5.9mm（验收线 2cm） | — |
| D5 | ~~方法章理论材料~~ ✅ 09-02 | ：三块推导+式号+代码出处对照+答辩三问预演 | — |
| D6 | ⚪ 固件 C 桌面测试 | protocol.c 编译成 DLL 与 Python 对拍（台架 B2 已覆盖，可暂缓） | 0.5 天 |
| D7 | ~~`pc/main.py` 增加 STATUS 接收（实车模式）~~ ✅ 09-09 代码完成 | **已实现**：`pc/status_rx.py` `StatusReceiver`（纯逻辑类，6 个测试 `tests/test_status_rx.py` 全过）+ `pc/main.py` live 模式绑定 UDP 9100 非阻塞收包（`drain_status`，replay 模式不建 socket）。坏帧整体包 `try/except ValueError` 计数不裸抛；滚动 1 秒 ≥5 坏帧 print 报警（协议字段失同步提示）；HUD 显示 MCU 模式/电压/电流/坏帧数；CSV 增加 mcu_mode/batt_v/rx_good/rx_bad 四列。背景：bridge 已把 STM32 帧经 `extract_one`（CRC 校验过）逐包 UDP 转发到 PC_PORT 9100；串口跨读边界重组已在 bridge 修复（abde261，`poll_serial_frames`）。剩余：实车联调验证（随 B7/B8） | 联调即验 |

---

## E. 外部依赖

| # | 项 | 状态 | 动作 |
| --- | --- | --- | --- |
| E1 | ~~git push~~ | 🟢 已恢复并推送（5fc9177），后续每次交付自动推 | — |
| E2 | 采购清单（2026-09-03 细化，含用途与参考价） | 🟡 用户已确认电推杆/继电器已有（现遥控器直控，未接 MCU） | **本周下单**（影响 B2/B3/B7 全线） |
| E2.1 | USB-TTL ×2（CP2102 款，带 3.3/5V 跳线） | ¥10-15/个 | B2 串口对拍必需 |
| E2.2 | ✅ 已选型（09-03 比价）：**RDK X5 基础套餐 4GB，¥1015**，大菠萝数码专营店（天猫 4.8 分）。含 64G 卡+散热片+读卡器+电源+Type-C。不选 8GB（负载用不满）；不选单主板/简配（缺散热必降频）。⚠️ 该店不支持 7 天无理由——**到货当天验机**：烧系统→SSH→推流→UART 四项全过再放。适配由 ZCode 负责 | ¥1015 | B7 车端网关；未来边缘推理零二次采购 |
| E2.3 | ~~NEMA17 驱动器 TB6600~~ | ❌ 09-05 取消：中间刀实为伺服+丝杆滑台（车上已装未接线，Hw-12），无步进可驱，不必购买（影响 B3 中间刀） | — |
| E2.4 | 5.8G 室外 CPE 网桥 **一对**（COMFAST E110A/E312A 类，点对点桥接）：PC 网口↔车端树莓派网口，免驱动 | ¥300-500/对 | B8 链路一票否决；比 USB 定向网卡可靠 |
| E2.5 | 光耦继电器模块 ×3 → **✅ 09-05 基本取消：电推杆已在车上、由现有外接继电器板控制**。剩一步：拍继电器板照片——①路数（双向换向需 6 路或板内已有换向逻辑）②输入触发极性/电平③现有触发源（遥控器还是原控制器），据此决定我们 6 路输出并联接入还是需切换电路 | ¥0 | 拍照发 ZCode 判读 |
| E3 | 弹齿刀采购 | 🔴 未下单 | 本周下单（对比试验刀头，W10 前到即可但不冒险） |
| E4 | 5GHz 定向天线/室外 AP | 🔴 未下单 | B8 不达标时的救命件，建议随 E2 一起买 |
| E5 | 导师认可创新点表述（v0.7 三点式） | 🔴 未沟通 | 下次见导师带上 v0.7 §7 |
| E6 | 田块协调（秋季除草作业许可） | 🔴 未确认 | 拍 C1 样本前就要落实 |

---

## 建议执行顺序（本周，09-05 复核）
1. **用户**：B6 实物确认（1h）→ E2/E3 下单 → B1 烧录 → B2 对拍（顺序不变：D1/D2/D3/D5 已完成，用户侧硬件链路仍是唯一阻塞）
2. **我**：无待写交付排队；D7 代码已完成（09-09），剩实车联调验证（随 B7/B8）
3. **并行**：C1 秋季样本现在就能拍（草还绿，每拖一周域差异风险越大）

---

## F+. P0 安全整改（2026-09-04，响应第二次外部审查）

> 审查证实三处 P0：① 安全门控补丁打在未被编译的 car_control.c 上，真实驱动路径（main.c pwm_veloc）无门控；② PC 急停/降级从不发 ESTOP 帧，固件把心跳当链路存活；③ 双 IWDG 并存。均已修复并重编译（Code=44544，0 错误）：
> - P0-1：门控移入 main.c 驱动点——AUTO 用 retrofit 斜坡轮速，ESTOP 四轮强制 0，MANUAL 原路径不变（含 retrofit_mode 补上缺失的函数体）
> - P0-2：pc/main.py 在进入 ESTOP/LIFT 的状态迁移瞬间显式发送 TYPE_ESTOP 帧（一次），固件锁存
> - P0-3：删 retrofit 重复 IWDG（见 F2 行）
> - P1 同批：限位开关→步进停（PD15 原点保持/PD10/11 方向闭锁）；MANUAL 刀默认提刀（TOOL 帧 500ms 新鲜度）；bridge 看门狗只在"PC 60 秒内活跃"时注入 ESTOP（纯遥控不被锁死）；图传改 MPEG-TS（OpenCV udp:// 可解码）；删除陈旧 firmware/大车/ 拷贝

## F++. 接线图与参数差异台账（2026-09-05）

- **`docs/接线图_v1.md`（v2 整车版）**：全车元件接线图（电源/行走/转向/作业/控制/人机六大系统），RDK X5 化。⚠ 接线前必查：① RDK 须独立 48→5V/4A 供电，严禁挂大板 5V 轨（§1-P5）；② PC6/PC7 兼接 485 收发器 U7、TTL 引出点待万用表敲定（B6-5）；③ T5 刀继电器脚号 7/9/11 有 ±1 行推断风险；④ PB5/PB6/PB7 步进引出点原理图未列出，可能需飞线；⑤ SL1 只断 MOTOR_1，后轴急停覆盖缺口待评估。**上电前先过其 §7 检查单**。
- **`docs/参数差异台账.md`**：实车与固件/PC 参数出入的统一登记处（Fw-1~10 / Pc-1~6 / Hw-1~7），含测法与验收线。**改参数一笔记一笔**；与 B4 台架标定互为索引。已登记：E2.2 车端改 RDK X5（Hw-6）、E2.3 步进驱动器 TB6600（Hw-7）。

## F* 第三次外部评审裁决（2026-09-05，8 条 P0 抽验 8/8 成立）

> 结论采纳：**当前禁止实车 AUTO、禁止按现有接线文档直接带动力接线**。评分 3/10 的判定"模块已写完≠系统已完成"成立。逐条实测裁决：

| # | 指控 | 验证证据 | 状态 |
|---|---|---|---|
| P0-1 | 论文按滑移转向建模，实车有四轮转向伺服 | docs/决策依据说明.md D8 写"实车没有转向轮偏转执行器"，与实车/固件(main.c:515 send_angle)矛盾 | 🔴 论文模型与 D8 决策须按实车重写（四轮转向+差速混合） |
| P0-2 | **STM32 永远进不了 AUTO** | ✅ 09-05 修复：NAV 语义=进入 AUTO（去掉 MANUAL 拦截）；HEARTBEAT=PC 存活但未指令→回落 MANUAL（遥控恢复）；协议显式模式字段留作 D 级增强 | 🔴→🟢 代码已改，✅ 已重编译(09-08,0 错误,61 警告均为遗留,Code=44616) |
| P0-3 | 在线推理归一化漏 /255 | ✅ 09-05 修复：抽出 preprocess_rgb() 强制 /255（tests/test_perception_norm.py 2 例）；F5 真实图像测试随之暴露 morph_process:302 除零，已加空墙回退 | 🔴→🟢 已修 |
| P0-4 | 视觉丢失/按键急停不发 ESTOP 帧 | ✅ 09-05 修复：prev_state 提前到帧首；键盘 E 路径补发 ESTOP 帧 | 🔴→🟢 已修 |
| P0-5 | 桥接冷启动锁死纯遥控 | ✅ 09-05 修复：last_pc=0 初始化（tests/test_control.py 3 例：冷启动不注入/见帧后超时注入/窗口过期停止） | 🔴→🟢 已修 |
| P0-6 | printf 污染协议帧 | ✅ 09-05 修复：usart.c fputc 置空（GBK 文件按 GBK 修补），集成说明"printf 让位"此句终于属实 | 🔴→🟢 已改，✅ 已重编译(09-08,0 错误,61 警告均为遗留,Code=44616) |
| P0-7 | 上电首 500ms 刀全落（判"新鲜"但 lift_bits=0）；ESTOP 不驱动 PA5；3 GPIO 不能完成 6 路换向 | apply_outputs 初值逻辑实测确认 | 🔴 与 Hw-9/9a 继电器板接入方案一并重做刀控 |
| P0-8 | 回放可误驱实车 | ✅ 09-05 修复：sock 仅 live 创建，replay 下 send() 为空操作 | 🔴→🟢 已修 |
| P1-1 | STATUS 无 PC 接收端（D7） | 已知未做 | 🟡 原 D7 计划 |
| P1-2 | STATUS 假数据（电流 0/电池 48.0 固定） | 已知（Fw-4/5）；U17 已确认贴片，具备接真条件 | 🟡 接 U17/U18 |
| P1-3 | tool_offset_px=None 崩溃 | ✅ 09-05 修复：AUTO 分支 None→0 | 🔴→🟢 已修 |
| P1-4 | RDK 未适配（stream_pi/ttyAMA0） | 已知（Hw-6） | 🟡 到货适配 |
| P1-5 | IPM 标定未接入运行时；5.9mm 是拟合误差非独立验证 | 已知（Pc-1）；"独立验证误差"批评采纳，标定后须用第 5 个点验证 | 🟡 C3 时做 |
| P1-6 | 时延只统计推理；视频无"只取最新帧" | 采纳；F3 分段打点已排期，最新帧机制补入 main.py | 🟡 |
| P1-7 | train_m.py 写死原仓库路径 | ✅ 09-05 修复：数据路径改为工作区相对解析 | 🔴→🟢 已修 |
| P1-8 | 桥不校验 UDP 来源/内容，垃圾帧可维持看门狗 | 采纳；帧 CRC 校验已在 bridge 转发前做，但"收到即续命"仍掩盖失联；改为仅 NAV/HEARTBEAT 有效帧续命+来源过滤 | 🟡 |
| 文档 | v0.7 DOCX/组会 PPT 与现状冲突；接线图 T5 三路=伸出/缩回表述过时 | 采纳：v0.7 降级为历史版本；PPT 6/15/16 页汇报前必须改；接线图 §4 已按外接继电器板方案更新，再复查一遍 U17/18、PD15 表述 | 🟡 |

**修复批次建议**（待用户点头即执行）：①10 行级速修 P0-2/4/5/6/8+P0-3+P1-3/P1-7（半天，全带回归测试）；②刀控/继电器板接入设计（等照片）；③论文/PPT 文档同步（等①完成后按新事实重写）。

## F. 外部评审采纳项（2026-09-03，另一 AI 评审筛选后采纳）

> 原评审 12 条批评，判定：采纳 5、简化 3、已覆盖 3、不采纳 1（IPM 在线倾角估计——过度工程，理论材料 4.x.3 的误差分解表为其边际收益提供反证）。完整判定记录见会话记录，此处只列进账。

| # | 采纳项 | 落点 | 优先级 |
| --- | --- | --- | --- |
| F1 | ~~指令斜坡过渡~~ ✅ 09-03 已实现并编译通过（SLEW_ACC=0.2 m/s²，1ms 步进，急停目标强制 0） | 固件 | — |
| F2 | ✅ 09-04 整改：撤销 retrofit 内重复 IWDG（同外设不可双实例），保留并依赖 main.c 原有 IWDG（1s，主循环喂）——retrofit/TIM6 若锁死主循环停喂同样触发复位，覆盖成立；复位后默认 MANUAL | 固件 | — |
| F3 | **时延分段打点**：协议状态/视频帧加 t_capture 字段，拆分 采集→传输 / 推理 / 执行 三段，分级前馈 | pc/ + common/protocol.py 载荷扩展 | 🟡 中（W4 链路通后） |
| F4 | **电机电流→刀具扰动前馈**：ACS712 电流作阻力观测，刀具 PID 加前馈项；做好了升级创新点 2 表述 | pc/control.py + 固件电流上报 | 🟡 中（台架后） |
| F5 | ✅ 09-03 实现，**09-04 按审查整改数字**：行投票裁剪 LSQ + 门控权重已接入生产管线（pc/perception.py，NaN 防护）。可复现数字（tests/test_adaptive_walls.py）：确定性稠密遮挡算例 25.5→0.5px；随机坡度套件 2.1→1.6px（仅裁剪贡献）；**真实掩膜 + 60×30 blob 模型下中点法本身仅 0.2px 误差（薄墙+LSQ 天然鲁棒），加权无必要也无害**。创新点 1 表述以确定性算例为量化证据，如实区分场景 | tools/morph_process.py, pc/perception.py, tests/ | — |
| F6 | 田间环境防护 checklist：镜头防尘罩、电路板三防漆、电机电源 EMI 磁环——并入 E2 采购与装配清单 | 硬件 | 🟡 中 |
| F7 | 试验设计修订：核心组（主动vs固定）5 趟重复，刀具组 3 趟；全部 ANOVA；CSV 增加草密度目测分级字段作协变量 | M4 试验执行 | 🟡 中（W10 前） |
| F9 | 对齐开题承诺（2026-09-05 读开题报告后补）：开题 2.3 承诺"霍夫变换 vs 最小二乘拟合对比试验"——将 F5 消融扩展为三方对比（Hough vs 朴素LSQ vs 行投票裁剪），离线即可完成，写进论文 4/5 章 | tests/test_adaptive_walls.py | 🟡 中（离线，随时可做） |
| F10 | 论文叙事锚点=开题四部分研究内容（平台与装置/导航线提取/解耦控制/田间试验），已逐项对应；两处调整（林下→大田、4WS→差速）已在组会 PPT 第 5 页主动说明 | 论文第 1-2 章 | — |
| F8 | 不采纳备案：IPM 在线倾角估计（过度工程）、硬件过流比较器（剪切销+软件阈值已双保险）、BiSeNet 备份（写展望） | 论文"不足与展望"章节素材 | — |

## F** 第四轮复核后的第二批修复（2026-09-05，评审 8 项阻塞中 5 项闭环）

| 阻塞项 | 修复 | 验证 |
|---|---|---|
| #2 上电前 500ms 刀全落 | retrofit.c：`last_tool_ms==0` 视为"从未收到 TOOL"，MANUAL 默认走提刀分支（两份拷贝） | ✅ 已重编译(09-08,0 错误,61 警告均为遗留,Code=44616) |
| #4 ESTOP 无物理断能 | retrofit.c `retrofit_poll_1ms`：锁存期间每 1ms 强制 `SW_Stop=0`（PA5→SL1 断 MOTOR_1 24V=刹车），压制原车主循环的松刹写入；**注意 SL1 仍只覆盖 MOTOR_1，后轴断能缺口待评估** | ✅ 已重编译(09-08,0 错误,61 警告均为遗留,Code=44616)+台架实测 |
| #5 AUTO 转向未接管 | main.c 门控 AUTO 分支强制 `angle[0..3]=0`（经各轮 correct=标定直行位）；遥控摇杆不再影响 AUTO（Fw-11） | ✅ 已重编译(09-08,0 错误,61 警告均为遗留,Code=44616)+台架定方向 |
| #6 LIFT 与 ESTOP 语义冲突 | pc/main.py：进入 LIFT 不再发锁存 ESTOP，改为持续 NAV(0,0)+TOOL(lift=0x07)——MCU 保持 AUTO 零速提刀、可恢复；PC 彻底失联仍由看门狗锁存；ESTOP 帧只留给横向超限/按键急停 | pytest 42/42 |
| #8 桥不校验 UDP | bridge.py 新增 `valid_frame()`：CRC 不合法帧既不转发也不喂看门狗（垃圾/伪造报文无法掩盖失联） | pytest 2 例（合法帧通过/垃圾与坏 CRC 丢弃） |

**仍未阻塞解除（依赖外部）**：#1 Keil 重编译（用户侧，编译后需重验 0 错误）；#3 继电器板照片；#7 STATUS 接收/真数据/RDK/IPM（D7/台架/C3/到货）。**带动力上车前置条件：Keil 重编译 0 错误 + 继电器板照片判读 + B2/B5 台架安全项全过。**

## F+* 第五轮复核修正（2026-09-08，评审指出 866572b 三处高估，均已返工）

| 评审指出 | 返工内容 |
|---|---|
| 转向接管位置错误（写在 send_angle 之后、被 path_plan 覆盖） | ✅ 移除门控内的死代码，接管改为紧贴 send_angle(4,...) 之前执行——AUTO 下四轮角度强制直行后再下发 |
| SW_Stop 1ms 强制仍有竞态（原主循环两个 tick 之间可写高） | ✅ 三处原车 `SW_Stop=` 写点全部加 `if (!retrofit_estop_latched())` 门控；1ms 强制保留为双保险；新增 `retrofit_estop_latched()` getter（retrofit.h/.c 双份） |
| 上电 PD4/5/6 未立即置安全电平 | ✅ retrofit_init 显式拉高三路（提刀），不等首个 1ms poll |
| bridge 仅 CRC 校验（合法帧可带尾随字节、无来源认证） | ✅ valid_frame 加精确长度校验（带尾随字节的整包拒绝）；config.yaml 新增可选 `allowed_sources` IP 白名单；+2 测试 |
| 刀具/继电器六路互锁与极性 | ⏳ 仍等继电器板照片（#3） |
| Keil 重编译 | ⏳ 仍用户侧（#1），编译前维持禁令 |

测试：**43 passed**（新增尾随字节走私拒绝 1 例）。**带动力上车前置条件不变：Keil 0 错误 + 继电器板照片 + B2/B5 台架全过。**

## F+** 第六轮复核修正（2026-09-08，评审指出 getter 链接回归，已修复）

- **P0 链接回归**：`retrofit_estop_latched()` 此前只在头文件声明、两份 .c 均无实现（补丁脚本 replace 锚点写错静默无操作）——已补实现并**写入后断言验证**；新增全量 C 符号自检：main.c 调用的 4 个 retrofit 符号全部有定义、头文件声明全部有实现、SW_Stop 4 个写点 3 个已门控+1 个是注释行。
- **来源白名单默认启用**：bridge 默认白名单 = 配置的 `pc_ip`（config.yaml 可用 `allowed_sources` 覆盖），非白名单来源的 UDP 一律丢弃。
- 仍未闭环（不变）：Keil 重编译（用户侧，编译前禁烧录）、继电器板照片、STATUS/RDK/IPM/田间闭环。

## F+*** Keil 重编译验证通过（2026-09-08）

- 用户在 Keil uVision(ARMCC 5.06u5)全量 Rebuild:**0 Error / 61 Warning(全部为原车遗留代码警告,retrofit.c/protocol.c/main.c 门控区零告警),Code=44616**,hex 已生成(OBJ/PWM.hex,本地)。
- 五轮评审的固件侧修复(P0-2/4/6/7、Fw-11、ESTOP 断能、上电提刀、getter)全部转为"已验证"。
- **带动力上车前置条件更新:①✅ 编译 ②⏳ 继电器板照片 ③⏳ B2 对拍(含 NAV=进 AUTO/HEARTBEAT=退 MANUAL/SW_Stop 断能实测)④⏳ B5 安全五项。**

### F+**** Keil 重编译验证通过（2026-09-14，命令行 headless，qoder 自跑）

- 机器上有 `D:\ruanjian\keil\KEIL32\UV4\UV4.exe`，用 `UV4 -r <PWM.uvprojx> -j0 -o build_log.txt` 全量重编，**不必再等用户手点 Rebuild**。
- 结果：**0 Error / 61 Warning（与 09-08 基线同数，未新增）/ Code=44840（09-08 为 44616，+224B）/ RO=1152 RW=1076 ZI=3068**，hex 已再生（`OBJ/PWM.hex`）。
- 交叉验证：`OBJ/PWM.map` 内 `retrofit_sbus_ok 0x08008863 Thumb Code 28 retrofit.o(.text)` 为**真定义**；`main.c` 的 `retrofit_sbus_ok declared implicitly` 警告消失 ⇒ 原型可见。
- 上一轮 `L6218E: Undefined symbol retrofit_sbus_ok` 根因：**中文路径 `大车proj/大车` 在 `git checkout/rebase` 时静默不生效**（命令报成功但文件没同步），已用文件系统 `cp` 修正（`f0ecf73`）。**教训：涉及中文路径的 git 同步，必须 `git diff` 到文件级核实，不能信命令返回码。**
- **带动力上车前置条件现状:①✅ 编译 ②⏳ 继电器板照片 ③⏳ B2 对拍(含 SBUS 失联两子情形) ④⏳ B5 安全五项 ⇒ 两条硬禁令仍未解除。**

---

## H. 外部整体审查（2026-09-09，codex·Astra）待办台账

> 报告全文：`docs/reviews/2026-09-09-project-review.md`（审查基线 94e1a99）。2026-09-11 逐条复验：**12 条中仅"STATUS 接收闭环"已被 D7 关掉，其余仍开**。前次复核中"tjcstr 已消失"的结论**有误**（main.c 为 GBK 编码，grep 按二进制跳过），Astra 原始判断正确，特此更正。

### H1. 🔴 数据资产安全（秋采 10 月中开拍前必修）
| # | 项 | 现状 | 证据 |
| --- | --- | --- | --- |
| H1.1 | ~~`split_dataset.py` 先清空后读源、路径指向只读仓~~ | ✅ 09-11 重写 | 路径改工作区、`_guard` 拒绝 JetBrains/工作区外、**先校验源再清空**、默认拒绝破坏性操作（需显式 `--force`）、`--dry-run`；测试 `tests/test_data_tools.py` 5 例 |
| H1.2 | ~~标注规范 `peony` vs `json2mask.py` 只认 `shaoyao`（会静默丢前景）~~ | ✅ 09-11 重写 | 冻结标签字典（`peony/shaoyao/芍药/shao_yao` 均认，background 同义），未知标签**报错**，全背景输出告警；测试 4 例 |
| H1.3 | 6:2:2 单图随机划分有邻帧跨集合泄漏风险 | 🟡 **机制已编码，泄漏未证伪** | 分组划分 + `split_manifest.json` 已实现（qoder `9e5774c`，按文件名首段分组）。**09-21 E1② 改掉两处**：恰 3 组时旧算式会让 test 集为 0，现在硬性三集合各≥1 组；组数 <3 从"告警并退回逐图"改成**直接拒绝**（正式评价不许静默降级）。同日补分组形态诊断与命名陷阱提示；**但分组清单要等 C1 秋采数据落地才能冻结**——"有分组功能"≠"邻帧不跨集合"，见 `docs/秋季图像采集清单.md` |

### H2. 🔴 控制权限与安全（上台架前必修）
| # | 项 | 现状 | 证据/方案 |
| --- | --- | --- | --- |
| H2.1 | MANUAL 失视觉未经请求切入 MCU AUTO | ✅ 已修（09-14；固件三项**已重编译 0 错误**，见下『H2 批次修复』表；剩烧录+台架复验） | `state_machine.py:43` vision_loss 对 MANUAL 无守卫；`main.py` LIFT 分支发 NAV(0,0)，固件 NAV⇒AUTO。需状态转换契约（适用模式×PC 状态×线上命令×MCU 模式） |
| H2.2 | AUTO 清零转角后仍叠加 `Switch_direction` | ✅ 已修（09-14；固件三项**已重编译 0 错误**，见下『H2 批次修复』表；剩烧录+台架复验） | `main.c:519` 清 angle 后 `send_angle` 仍 ±Switch_direction；需在执行处统一仲裁全部转向输入（4WS 解锁的前置） |
| H2.3 | MANUAL 缺 MCU 侧遥控失联停车 | ✅ 已修（09-14；固件三项**已重编译 0 错误**，见下『H2 批次修复』表；剩烧录+台架复验） | `retrofit.c` 仅 AUTO 查 `last_pc_ms`；`sbus_last_ms` 无读取方。断接收机/SBUS 线后旧指令仍被使用 |
| H2.4 | `tjcstr[30]` 缓冲区溢出 | ✅ 已修（09-14；固件三项**已重编译 0 错误**，见下『H2 批次修复』表；剩烧录+台架复验） | `main.c:266` 定义 30B；`:303/:331` sprintf 实际写 37B+ NUL=38B（GBK），越界 8B。改有界格式化 |
| H2.5 | ~~bridge 注入 ESTOP 覆盖 `last_pc_ms`~~ | ✅ 09-14 修复 | `bridge.py:49` 注入时仍 `last_pc_ms=now_ms` → 真实收帧时间被自身注入覆盖，连续注入无退出窗口。✅ 已拆分：`last_real`（仅真实帧刷新）与 `last_inject`（节流 500ms）；回归测试复现评审场景——70s 连续轮询下注入限频、60s 窗口过期即停止、真实时间戳不再被推进 |
| H2.6 | ~~bridge `poll_serial_frames` 丢弃 rest~~ | ✅ 09-11 已修 | `frame is None` 时先取 `rest` 再判断；半帧保留、噪声前缀只留末字节；新增 `tests/test_control.py` 跨边界 4 例 |

### H3. 🔴 "定型"未落到生产路径（live 链路）
| # | 项 | 现状 | 方案 |
| --- | --- | --- | --- |
| H3.1 | live 采集仍是打不开的 UDP 路径 | ✅ 已修（09-14，qoder R3 并入主线） | `pc/main.py:70` 仍 `cv2.VideoCapture('udp://@:5000')`（09-10 实测打不开）；`vehicle/stream_pi.py` 仍 MPEG-TS/UDP。应切 TCP multipart MJPEG（`tcpserversink` + `tools/stream_view.py` 的接收逻辑并入 `_from_stream`） |
| H3.2 | `vehicle/config.yaml` 仍是树莓派配置 | ✅ 已修（09-14，ttyS1/127.100/TCP） | `serial_dev: /dev/ttyAMA0` → 应 `/dev/ttyS1`；`pc_ip: 192.168.1.2` → 实际网段；stream 段改 TCP |
| H3.3 | eth0 换链路对端偶发挂死 | 🟡 未查 | 09-11 网桥验证中复现，重启恢复；怀疑与"管理 192.168.2.x + 数据 192.168.127.x 同卡双网段"有关 |

### H4. 🟡 论文/证据口径（送审前）
| # | 项 | 现状 |
| --- | --- | --- |
| H4.1 | ~~ch04 NAV2/4WS 现在时描述 + 步进勘误 + ch03 峰值口径~~ | ✅ 09-14 关闭（qoder 三条手术提交落 chucao_prj be9093a：ch04 前轮Ackermann+NAV2 标设计待实现+Hw-11 前置、中间刀/除草装置 步进→48V 伺服、ch03 全程峰值第76轮0.7459）。剩余 H4.4 数据清单/H4.6 理论推导/H4.7 仓卫生，见下 |
| H4.2 | 中间刀执行器论文写"步进"，实车是伺服 | ✅ 已改（09-14，chucao_prj be9093a） |
| H4.3 | ch03 "epoch 201 mIoU 峰值" vs CSV 全程峰值 epoch 76=0.7459 | ✅ 已改（09-14，be9093a） |
| H4.4 | `data_manifest.md` 空表；权重/配置/日志/图表未绑定运行 ID | 🔴 待补 |
| H4.5 | README 称 `test_protocol.py` 为"Python↔C 一致性测试"（名不副实） | ✅ 09-11 已改措辞 |
| H4.6 | 方法章理论材料三阶多项式推导错误（实为二阶）；代码用航向项而主入口未传航向误差 | 🔴 待重推 |
| H4.7 | 只读仓 `JetBrains/chucao_prj` 被论文提交写入且当前脏树 | 🔴 待清理（见 AGENTS.md §只读约定） |

### H5. 🟡 功能完成度（Astra 列，部分已推进）
| # | 项 | 现状 |
| --- | --- | --- |
| H5.1 | ~~STATUS 无 PC 接收闭环~~ | ✅ D7（09-09）已建；剩"R 键物理复位是否真到 MCU"待台架验 |
| H5.2 | IPM 仍用占位几何，不加载标定文件 | 🟡 **加载器已建 + 生产者已写来源，接线仍故意推后**。**09-21 查清**：`results/smoke_ipm/ipm_params.json` **不是标定**（源图 `frame.jpg` mean=0/std=0 全黑，"reproj 0.00588" 只是求解器把自己的合成输入解回去）→ **仓库现存零个真实外参标定**。已交付 `pc/ipm_io.py`（来源校验加载器，10 测试：含"拒绝那份冒烟产物"、含**跑真实 `solve()` → 真实 `load_ipm_params()` 的闭环**、含复审 §4.2 的 NaN 前视距离／负重投影误差／h22≠0 但不可逆三个探针）；`calib_ipm.solve()` 现在写出 `source_image / points_source / vehicle_profile / calib_date / n_points / pixel_space`，并拒绝近乎全黑的源图。**09-21 先建加载器、后改标定器，中间加载器会拒自己标定器的产物，这条断链现已闭合**。**⚠️ 09-21 复审订正本行旧解释**：此前写"接进来等于把 `px_to_meters` 改 6.7 倍，`ESTOP_LAT_LIMIT=0.05 m` 等所有米制阈值语义同时变，必须与阈值重审同批做"——**不成立**。`scale_px_per_m=100`（m/px=0.01）是**求解脚本自己选的 BEV 绘图分辨率**，不是标定发现的物理常数；只要变换与换算自洽，5 cm 既写成 5 px × 0.01 m/px，也写成 33.33 px × 0.0015 m/px，**物理距离相同**，所以换真标定**不要求把米制阈值按 6.7 倍缩放**（`main.py:34` 的阈值本来就是米，它不需要跟着重采样比例改）。真正必须同批做的是三件事：① px→m 的换算要来自**经过独立检验的 H 与其声明的像素坐标系**（旧占位是拍出来的固定线性比例，且 0.0015/0.0030 横纵各向异性）；② 旧占位投影换成真投影后**控制效果重新验证**（回放＋台架，不是改阈值数字）；③"横纵向等比例"是本实现的选择，不是所有有效 BEV 的必要条件。前置：先在上代车＋平地拉线做出一份真标定 |
| H5.3 | 时延只测感知处理，无采集时间戳/帧龄拒绝 | 🔴 未做 |
| H5.4 | 刀控/电流（固件返回 0）/电池（固定 48V）未闭环 | 🔴 等硬件（P0-7 + 继电器板照片） |
| H5.5 | `train_m.py` test 分支引用未定义 `val_*` 变量（NameError） | ✅ 已修（09-14，qoder） |

### H1+（2026-09-14 复审（qwen3.8，报告 docs/reviews/2026-09-14-project-re-review.md）新增项）

| # | 项 | 现状 | 证据/方案 |
| --- | --- | --- | --- |
| H1.4 | ~~训练权重输出仍写只读仓 `SAVE_WEIGHT_DIR=D:/JetBrains/chucao_prj/...`（import 即 makedirs）~~ | ✅ 09-14 修复 | `train_m.py` SAVE_WEIGHT_DIR 改 `_WS/model_data/weights`。**前次"P1-7 解除 train 禁令"只修了输入、漏了输出/续训**（训A用B错配风险同步消除） |
| H1.5 | ~~split_dataset force=False 重跑混写（7/4/3 交叉复现，img_4/img_8 双集合）~~ | ✅ 09-14 修复 | 非空目标且未 --force → **写入前整体拒绝**；force 先清后写。回归：`tests/test_data_tools.py` 2 例（复现评审 7/4/3 场景） |
| H1.6 | split 输出目录(`model_data/dataset`)与 train_m 读取(`model_data/{train,val,test}`)不衔接；掩码不随 split 复制 | ✅ 已修（09-14，qoder 输出对齐） | 需统一数据配置与准备顺序 README，检查每图有对应 mask |
| H1.7 | 分组清单未冻结（原视频/地块/采集日），邻帧泄漏未证伪 | 🟡 **机制就绪，内容待数据** | `split_dataset.py` 已按组划分并落 `split_manifest.json`；**缺的是真实分组清单（等 C1 秋采）**，与 H1.3 同源，秋采前冻结 |

### H2 批次修复（2026-09-14，H2.1-H2.4 代码完成，✅ 固件已重编译 0 错误，剩烧录+台架）

| # | 项 | 状态 | 实现 |
| --- | --- | --- | --- |
| H2.1 | MANUAL 失视觉擅入 AUTO | ✅ 代码+4 测试 | `vision_loss()` 仅 AUTO 生效（MANUAL 是遥控权威，失视觉只作 HUD 警告）；LIFT 仅从 AUTO 可达，线上 NAV(0,0) 不再在 MANUAL 出现。契约注释写在 state_machine.py |
| H2.2 | AUTO 残留 Switch_direction | ✅ 代码 + **已编译** | `main.c` 四个 send_angle 在 AUTO 下仲裁 Switch_direction=0（校准修正 wheel_Angle_correct 保留）；MANUAL 行为不变 |
| H2.3 | MANUAL 缺 SBUS 失联停车 | ✅ 代码 + **已编译** + ⚠️ 09-14 复审发现原修法失效，已改 | `retrofit.c` 新增 `retrofit_sbus_ok()`（sbus_last_ms 500ms 新鲜度，首发帧前为 false）；`main.c` 驱动点 MANUAL+失联 → 四轮 veloc=0（可自动恢复、覆盖 MOTOR_2，补 Hw-2 的单侧缺口）。**复审追心跳写入方发现：`telecontrol.c` 的 `retrofit_notify_sbus()` 原挂在 USART1 IDLE 中断开头、在 `0x0F` 帧头+XOR 校验之前，任何噪声/浮空线上的乱字节都会刷新 `sbus_last_ms` → 失联保护被架空。现已移入校验通过分支内。** |
| H2.4 | tjcstr[30] 溢出 | ✅ 代码 + **已编译** | 缓冲 30→48；**同文件全部 17 处** `sprintf(tjcstr,…)` → `snprintf(tjcstr, sizeof,…)`（含评审未列的 14 处，彻底消除复发面） |
| ✅ | **Keil 重编译（09-14 qoder 命令行 `UV4 -r` 完成）** | 🟢 **0 Error / 61 Warning（与 09-08 遗留基线同数，未新增）/ Code=44840（原 44616，+224B）/ hex 已再生 / `PWM.map` 含 `retrofit_sbus_ok@0x08008863` 真定义**；`main.c` 的 `retrofit_sbus_ok declared implicitly` 警告消失，证明原型已可见 | 证据文件：`firmware/大车proj/大车/USER/build_log.txt`。上一轮 `L6218E` 未定义符号的根因是中文路径 `大车proj/大车` 在 checkout/rebase 时静默不生效，已用文件系统拷贝修正并入主线 |
| 🔴 | **烧录 + B2/B5 回归** | 用户侧 | 重编译已过 ⇒ 允许上台架（此前"0 错误前不得上台架"的门槛现已满足）；B2 对拍需加验：MANUAL 断 SBUS 的 **(a) 关接收机 (b) 线断悬空出乱码** 两子情形→四轮停、SBUS 恢复→可继续；横移中切 AUTO→四轮立即回标定直行位（无 90° 残留） |

**组合场景验收清单（H2 关闭条件，不以测试数为准）**：① MANUAL+失视觉→线上只有 HEARTBEAT；② AUTO+失视觉→NAV(0,0)+TOOL 提刀；③ 横移(Switch_direction=90°)→切 AUTO→四轮角无残留；④ MANUAL 断 SBUS **两种子情形都要测**：(a) 关接收机/拔线=线上无电平跳变→≤500ms 四轮停；(b) 线断但悬空/感应出乱码→同样≤500ms 四轮停（09-14 复审前 (b) 必失效，需重点复验）；⑤ SBUS 恢复→手动可控；⑥ 连续 2h 桥接运行无重复注入；⑦ **【已知遗留】MANUAL 失联只清零 veloc（驱动力），`send_angle` 仍按最后一次 angle[] 指令转轮**——车速已为 0 时残留转角无危险，但台架上会看到轮子保持最后角度，验收时按"可接受"记录，不得当作失联已完全仲裁。

### H2+（2026-09-14 复审新增）

| # | 项 | 现状 | 证据/方案 |
| --- | --- | --- | --- |
| H2.7 | ~~Ackermann4WS 几何不自洽~~ | ✅ 09-14 修复（前轮 Ackermann+后轮直行，统一 ICC；+5 几何测试 `tests/test_nav_geometry.py`；策略=前轮-only 不作创新点） | `network/nav_control.py:134-155`。**影响路线甲**（v0.9 拟复用该类、ch04 抄同类公式）。修法：先定转向策略（前轮-only/对称4WS/后轮反相），从同一 ICC 推导四轮角+轮速；补左右镜像、δ→0 直行极限、刚体速度一致性三条单元测试 |
| H2.8 | ~~STATUS 接收无新鲜度/序号~~ | ✅ 09-14 代码修复（`status_rx.py`：STALE_S=1s 失联判 stale、u16 序号新旧检查防旧帧覆盖、坏帧+失联双报警；剩 AUTO 许可叠加 MCU 新鲜度 → 状态契约轮） | `status_rx.py` 需加 last_rx_ts/seq 单调检查/失联超时；`main.py` AUTO 许可需叠加 MCU 状态新鲜度与急停锁存。"接收器已实现"≠"状态闭环已验收" |
| H2.9 | 验收口径错误：X5 清单曾让用 main 的 latency 列验收控制帧 p95<50ms | ✅ 09-14 已改 | 该列计时始于取帧后=感知耗时，非控制往返；rx_good/bad 非丢包分母。**视频同框/网络 RTT/控制帧往返/感知耗时四项分开测** |

**建议顺序（与 Astra 一致，2026-09-14 复审认同并微调）**：H1 护资产 → H2 控制权限 → H3 live 链路 → H5 工程细节 → H4 论文口径。R4 几何与反馈前提（Hw-11 轮角反馈是否真可用）是 4WS/NAV2 的前置门，先修几何+定反馈再谈 NAV2。

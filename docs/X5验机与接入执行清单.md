# RDK X5 验机与接入执行清单（2026-09-09）

> 覆盖：到货验机（E2.2 四项）→ B1 烧录 → B2 串口对拍 → B7 车端接入 → B8 链路打流。
> 顺序执行，每项做完在【回填】写结果。所有命令在 Windows PC（Git Bash/PowerShell）或 X5 SSH 终端执行。
> 依据：官方文档 developer.d-robotics.cc（v3.5.0）+ 本仓库缺口清单 B2/B7/B8。

---

## 0. 准备

| 物品 | 状态 |
| --- | --- |
| RDK X5 套餐（含 64G TF 卡 + 读卡器 + 电源） | ✅ 已到 |
| USB-TTL ×2（CP2102，3.3V 挡） | ✅ 已到 |
| CPE 网桥一对 | ✅ 已到（第 5 步才用） |
| 网线 ×1 | 需确认 |
| 回形针/镊子（TF 卡座弹簧） | — |

**关键事实**（官方文档核实）：镜像 ≥3.0.0 = Ubuntu 22.04；SSH `sunrise/sunrise`；串口 `root/root`；eth0 出厂静态 IP **192.168.127.10/24**；40pin Pin8=UART1_TX、Pin10=UART1_RX，**3.3V 电平**（与 STM32 PC6/PC7 直连无需转换，共地即可）。

---

## 1. 烧录系统（E2.2 第 1 项）

1. 下载镜像：https://archive.d-robotics.cc/downloads/os_images/rdk_x5/ → 选**最新 3.x 版本**（Ubuntu 22.04），下载 `.zip` 并解压出 `.img`。
2. 读卡器插 TF 卡 → **Rufus**（https://rufus.ie）→ 选卡 → 选 img → 写入（DD 模式弹窗选确定）。
3. 卡插 X5 卡槽（弹簧卡座，推到底），接电源开机，等 1-2 分钟。

**验收**：板上 LED 正常（红灯常亮/蓝灯闪烁属正常启动序列，以说明书为准）。
【回填】：

## 2. 网线直连 + SSH（E2.2 第 2 项）

1. PC 网口设静态 IP：`控制面板 → 网络 → 以太网 → IPv4`：
   - IP `192.168.127.100`，掩码 `255.255.255.0`，网关留空
2. 网线连 PC ↔ X5 网口，`ping 192.168.127.10` 通。
3. SSH：`ssh sunrise@192.168.127.10`（密码 `sunrise`）；首次确认输 `yes`。
4. 查版本：`cat /etc/version`、`rdkos_info`。

**验收**：SSH 进去，版本 ≥3.0.0。
【回填】：

## 3. UART 自发自收（E2.2 第 4 项提前做，X5 单板即可验）

1. SSH 后装 pyserial：`sudo apt update && sudo apt install -y python3-serial`
2. 列串口：`ls /dev/ttyS*`；`dmesg | grep -i ttyS` 看哪个对应 40pin UART1（预期 `/dev/ttyS1`，Pin8/10）。
3. 若串口被 console 占用或无设备：`sudo srpi-config` → Interface/40pin 复用里启用 UART（以菜单实际项为准）。
4. **短接 Pin8 与 Pin10**（杜邦线），跑：

```bash
python3 - <<'EOF'
import serial
ser = serial.Serial('/dev/ttyS1', 115200, timeout=1)
ser.write(b'PING123')
print('rx:', ser.read(7))
EOF
```

**验收**：打印 `rx: b'PING123'`。
【回填】：

## 4. B1+B2：固件烧录 + PC↔STM32 对拍（USB-TTL #1，大板桌面完成）

1. **B1 烧录**：Keil `Flash → Download`（或 STM32CubeProgrammer 烧 `firmware/大车proj/大车/OBJ/PWM.hex`，注意 09-08 重编译后的 hex：0 错误，Code=44616）。
   - 验收：上电行为与烧录前一致（遥控可开、刀不动，MANUAL 默认态）。
2. **接线**：USB-TTL 插 PC → 大板 USART6：PC6(TX)→TTL RX、PC7(RX)→TTL TX、**共地**；跳线帽确认 3.3V 挡。
3. **对拍**：设备管理器查 COM 号（如 COM5）→

```bash
python common/uart_vectors.py COM5
```

**验收**（缺口清单 B2）：① 6 组有效帧不崩、0x10 状态帧 20Hz 出现；② `bad_crc_must_reject`/`truncated_must_reject` 静默丢弃；③ PC 收到的状态帧 CRC 全过。
【回填】：

## 5. B7：X5 接入链路（E2.2 第 3 项推流 + UART 换线）

1. **推流验证**（先网线直连）：X5 上 `ls /dev/video*` 确认相机（USB3.0 UVC 相机接 X5 USB3.0 口）→ 跑本仓库 `vehicle/stream_pi.py`：

```bash
# X5 上（repo 传过去：scp -r 或 git clone）
sudo apt install -y gstreamer1.0-tools gstreamer1.0-plugins-{base,good,bad,ugly}
python3 vehicle/stream_pi.py 192.168.127.100
# PC 上（Git Bash）：
gst-launch-1.0 udpsrc port=5000 ! application/x-rtp ! rtph264depay ! avdec_h264 ! autovideosink
```

2. **UART 换线**：拔 USB-TTL，改接 X5 40pin：Pin8(TX)→大板 PC7(RX)、Pin10(RX)→大板 PC6(TX)、Pin6 GND→共地（**3.3V 直连，无需转换**）。
3. **改配置**：`vehicle/config.yaml`：`serial_dev: /dev/ttyS1`（以第 3 步实测节点为准）、`pc_ip: 192.168.127.100`（网桥阶段改为 PC 网桥侧 IP）。
4. **跑 bridge**：X5 上 `python3 vehicle/bridge.py`；PC 上 `python pc/main.py --live --pi-ip 192.168.127.10 --no-gui`（先别进 AUTO，看 STATUS 收包：HUD/CSV 出现 mcu_mode/batt_v 即 D7 链路通）。

**验收**：PC 出实时 720p 图（秒表对比法时延 <100ms）；STATUS 帧经 X5 转发到 PC，rx_good 持续增长、rx_bad=0。
【回填】：

## 6. B8：网桥替换网线 + 打流（一票否决）

1. PC 网口改接网桥 LAN；两台网桥按说明书配对（默认出厂多即插即用，同网段透明桥——不通再进网桥管理页配 SSID/信道）。
2. X5 IP 仍为 192.168.127.10，PC 恢复 192.168.127.100，`ping` 通即可。
3. **打流**（田间距离 120m 模拟：先把两台网桥拉开实际作业距离再测）：

```bash
ping -n 100 192.168.127.10        # Windows
# 控制帧打流：pc/main.py --live 跑 10 分钟，看 rx_good/rx_bad 与 latency 列
```

**验收**：丢包 <1%，控制帧 p95 <50ms；**不达标 → 调网桥朝向/信道，直至达标才准进 M2**。
【回填】：

---

## 已知坑位（来自台账，测试时对照）

- **printf 污染**已修（fputc 置空），若状态帧出现乱码字节先查这个回归。
- **坏帧报警**：PC 端若 HUD 出现 `bad:` 计数增长或 1 秒 ≥5 报警 → 固件/Python STATUS 字段失同步，停下对 0x10 帧长度（14 字节）。
- **bridge 看门狗**：PC 60 秒无帧才注入 ESTOP，纯遥控测试时不会被锁死（F+ P1）。
- **E2.5 待确认**：拍刀继电器端子照片（P0-7 刀控重做的输入），本次上车顺手做。

## 验机四项结论（E2.2 约定：全过再放，不过 7 天内退换——该店不支持无理由）

| # | 项 | 结果 |
| --- | --- | --- |
| 1 | 烧系统 | ✅ RDK OS 3.5.0 / Ubuntu 22.04.5，x5_rdk_v2 |
| 2 | SSH | ✅ 网线直连 192.168.127.10，sunrise/sunrise |
| 3 | 推流 | ✅ TCP multipart MJPEG，30.5fps，**端到端 105ms**（秒表同框法，2026-09-10） |
| 4 | UART | ✅ **/dev/ttyS1**（40pin Pin8/10），115200 回环 PING123 |

> 验机过程中的实测教训：① 相机 USB 线材/接口接触不良反复掉设备（dmesg: error -71），**上车必须换优质 USB3.0 短线并直插蓝口**（并入 F6 防护清单）；② OpenCV 5.0 的 FFMPEG 后端打不开中途加入的 UDP TS 流（isOpened False 30s 超时），且 MJPEG 整帧 118KB 超 UDP 65507 上限——**视频链路定型为 TCP multipart MJPEG**（`tools/stream_view.py` 接收，105ms 实证）；VLC 缓冲 ~1s 不可用于时延验收。

# 论文文本改动清单（R7 / H4，2026-09-14）— 待授权应用到 chucao_prj

> 目标文件：`D:\JetBrains\chucao_prj\thesis_cau\03_chapters\ch03_draft.md`、`ch04_draft.md`、`ch02_draft.md`
> 依据：v0.9 §3.1（R4 定案：前轮 Ackermann + 后轮=0）、复审 09-14 R7、台账 Hw-7（中间刀=伺服非步进）、Hw-11（轮角反馈待核实）。
> **状态：本清单在 qoder 侧；chucao_prj 标"只读"，未擅自改。用户点头后由我或 zcode 落库。**
> 改完全库 grep 复核：`后轮反向|反向偏转|步进电机|NEMA17|NAV2.*下发|epoch 201|峰值` 应无残留（显式调整声明除外）。

---

## A. ch04 §4WS 阿克曼运动学建模（第 16–32 行）—— 与 R4 修后代码一致

**A1 第 16 行**：
- 旧：`…四个车轮绕同一瞬时转动中心（ICC）转动的阿克曼几何成立。`
- 新：`…前轮按阿克曼几何绕同一瞬时转动中心（ICC）转动，后轮保持直行（转角为 0）。`

**A2 第 20 行**：
- 旧：`四轮转角按阿克曼几何分配：前外轮与后轮（后轮反向偏转）分别为`
- 新：`前轮转角按阿克曼几何分配，后轮转角恒为 0：`

**A3 第 24 行（后轮反相公式，R4 的错源）**：
- 旧：`[[EQ:\delta_{rl}=-\arctan\frac{L}{R-B_r/2},\quad \delta_{rr}=-\arctan\frac{L}{R+B_r/2}]]`
- 新：`[[EQ:\delta_{rl}=\delta_{rr}=0]]`

**A4 第 26–30 行**：后轮半径式 `R-B_r/2 / R+B_r/2` **保留**（后轮直行时其到 ICC 半径仍是该值，与 `nav_control.py` 修后一致）。第 30 行 NAV2 由**现在时→设计/待实现**：
- 旧：`导航控制帧（NAV2）下发的即为四轮目标转角与中心速度 v：车载固件将目标角经斜率限幅写入转向舵机，并按上式将中心速度映射为四轮目标转速，进入既有 PID 速度环。`
- 新：`导航控制帧（NAV2，本协议为设计、待实现）下发前轮目标转角与中心速度 v，后轮目标角置 0；固件将目标角经斜率限幅写入前转向舵机，并按上式把中心速度映射为四轮目标转速，进入既有 PID 速度环。`

**A5 第 32 行（转向失效检测）**：标"待实现 + 反馈前置"：
- 旧：`同时固件利用转向角反馈实现转向失效检测（指令角与反馈角偏差超阈值持续超时即触发减速、提刀、停车）…`
- 新：`并规划由固件依据转向角反馈实现转向失效检测（指令角与反馈角偏差超阈值持续超时即减速、提刀、停车）；该检测依赖 MCU 可读取的轮角反馈信号，其可得性待核实（台账 Hw-11：原车外部轮角采样链是否退役、伺服内部闭环能否读回）。`

---

## B. ch04 §中间刀对行 + ch02 §除草装置 —— 步进 → 伺服（Hw-7）

**B1 ch04 第 60 行**：
- 旧：`中间刀横向滑轨由步进电机驱动…执行机构运动学分辨率为 s/(200N_s)（s 为丝杠导程 8 mm，N_s 为细分数）…`
- 新：`中间刀横向滑轨由 48V 直流伺服（位置模式，电子齿轮等效脉冲）经滚珠丝杠（导程 8 mm）驱动…横向分辨率由伺服编码器与电子齿轮决定，远高于 ±2 cm 对行需求，控制精度瓶颈在感知端而非执行端。`（删"200N_s 细分数"式）

**B2 ch02 §除草装置总体布置**：`NEMA17 步进电机` → `48V 直流伺服（与转向同型）`；快换/导轨描述不变。台账 Hw-7/Hw-12 为依据。

---

## C. ch03 §分割模型训练与评估 —— mIoU 峰值口径（复审 R7）

- 旧：`第 201 轮取得峰值 mIoU 0.7163…`
- 新：`第 201 轮 mIoU 0.7163（该阶段精调末值）；全程验证集峰值为第 76 轮 0.7459（见训练日志）。` —— 不再把 201 轮称"全程峰值"。

---

## D. 一致性收尾
- ch02 §系统总体架构 / 表 2-2 若仍含"滑移转向/无转向机构"→ 改 4WD-4WS（前轮转向 + 后轮回正），并保留"前期曾误判、据固件核对修正、与开题一致"的显式调整声明。
- 摘要/结论若提"四轮转向控制"→ 与"前轮转向 + 后轮回正"口径统一。

*本清单为纯文本 before→after，套用时逐条 diff 即可；应用方：用户授权后 qoder 或 zcode 落 chucao_prj。*

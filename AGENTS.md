# 项目：纯视觉芍药行间除草机器人（硕士论文样机）

本工作区是唯一开发区。`D:\JetBrains\chucao_prj` 等原仓库**只读，禁止修改**。

**唯一例外（2026-09-14 授权，qoder 申请、zcode 批准）**：`D:\JetBrains\chucao_prj\thesis_cau\` 子树 = 论文开发区，**可写**。
- 例外边界：**仅限 thesis_cau/**；chucao_prj 的其余部分（network/、model_data/、data/、标定资产、训练日志）**仍然只读**——只读规则的本意是护研究资产，不是冻结论文写作。
- 提交纪律：只 `git add thesis_cau/...` 做手术式提交（该仓工作树有其他未提交改动，**严禁一并带入**）；每条改动独立 commit，信息注明对应评审项（R7/H4.x）。
- 改完向用户报 chucao_prj 的 HEAD 哈希；参数性事实（如"全程峰值 0.7459"）以台账/CSV 为据。

## 事实权威序（铁律，2026-09-12 立）
**设备手册/用户实测/现场确认的硬件事实 = 最高权威**，登记 `docs/参数差异台账.md`（Hw/Fw/Pc 项）。
AI 只能引用该台账，**不得再自行推测覆盖**；查不到时明确标注"待确认"，禁止把"查不到"或"间接推断"包装成"已核实"。
文档与事实冲突时，改文档，不改事实。
（教训：2026-09-12 就 X5 供电连续出错三次——"12V 会烧板"（查不到官方页的推测）、"适配器多档 PD"（从间接信息反推）、"必须 PD 协商"（超出手册原文）。最终以**设备手册原文 5V/5A**定案。→ 规格类问题一律先查手册/实测；引用手册时只写手册写了什么，不替它加条件。）

## 角色分工（2026-09-14 起，用户指定）

- **qoder = 主开发**：写代码、写方案/论文文档、出 diff。
- **zcode（Claude）= 审查与验收**：review qoder 的每个 diff，把关后 merge 进 zcode 主线并推 GitHub。审查者**不直接写功能代码**，但可以修审查中发现的最小阻断项（随 review 附说明）。

### 审查清单（每个 diff 必过，2026-09-14 立）
1. **事实核对**：涉及硬件/规格/数值的，逐条对 `docs/参数差异台账.md` 与设备手册——禁止接受"推测当事实"。
2. **闭环核对**：声称修了 X，就要验证 X 的**原始症状**消失，而不是只看新增测试数；跨模块改动（PC状态→线上帧→MCU模式）要求组合场景覆盖。
3. **资产边界**：diff 不得触及只读仓（唯一例外 `chucao_prj/thesis_cau/`）；工作区外路径一律拒绝。
4. **编码坑**：firmware/*.c 是 **GBK**——检查 diff 是否用字节级方式修改（普通 grep/Edit 工具会漏检/写坏中文）。
5. **口径核对**：测试数/完成度表述不得超出证据（"已设计/已编码/已台架/已田间"四态分列）；时延类指标按 B8 测量口径四项分开。
6. **配置一致性**：改代码必须同步 config/README/台账（串口名、IP、路径、型号），消灭单一事实源漂移。
7. **固件改动**：必须附"待 Keil 重编译"标记，重编译 0 Error + B2 加验前不得上台架。

### 流程
qoder 完成一批 → 向用户报 HEAD + 改动清单 → 用户转发 → zcode fetch、审查（跑测试 + 逐项过上面清单）→ 意见回 qoder 或 merge/push → 报主线 HEAD。
zcode 自己的提交（审查修正、台账、文档）走正常 commit/push，同样接受 qoder 反查。

## 双线并行同步协议（2026-09-14，zcode ↔ qoder 两仓均可写）
- 双仓已互设 remote：zcode 侧 `qoder → D:\ai work\qoder`；qoder 侧 `zcode → D:\ai work\zcode`。
- **每次开工前**：`git fetch <对侧> && git merge <对侧>/master`——有冲突先解决，不解决不许开新工。
- **每次收工后**：commit → push origin → **向用户报 HEAD 哈希**，由用户转发对侧。
- 文件分区（按 09-14 角色分工）：qoder 写代码 diff、v0.9 方案（docs/superpowers/specs）、论文文本；zcode 出审查意见、维护台账/AGENTS/执行清单。两边都会动的文件（AGENTS/缺口清单）合并时以"条目不丢"为原则，不比措辞。
- 合并方向：qoder 的新修复以 **merge 进 zcode 为准**（zcode 绑定 GitHub 主线），qoder 侧随后 rebase 或重新 fetch。

## 当前状态（2026-09-09，P0 修复批次完成后）
- **仍然有效的硬约束：禁止实车 AUTO、禁止按现有接线文档直接带动力接线**（评分 3/10 的"模块已写完≠系统已完成"判定成立，见 docs/缺口清单.md §F*）。解除条件：B5 安全动作实测全项通过。
- ~~禁止运行 network/train_m.py~~ 已解除（P1-7 修复：训练路径改为工作区相对路径），但需自备 `model_data/weights/` 与数据集。
- **旧批次 P0 已修**（09-08：P0-2/3/4/5/6/8 + P1-3/P1-7，带回归测试；r5/r6 链接回归修复；上电提刀；ESTOP 1kHz 制动）——**注意这只代表"那一批"关闭，不代表当前准入**。当前准入缺口见 `docs/缺口清单.md` §H 与顶部『📊 总览』板。**H2 六条（MANUAL→AUTO、Switch_direction、SBUS 失联、tjcstr、bridge last_pc_ms、STATUS 联锁）09-14 已全部做到「已编码 + 已编译」，之所以仍算开放是因为「未台架」，不是因为没写**；其中 SBUS 失联一条 09-14 复审时发现原修法被浮空线乱码架空，已改（Hw-17 登记遗留）。**"N 项测试通过"不作为 H2 关闭理由**，需组合场景 ①-⑦ 覆盖。
- **固件已重编译**（**09-14 qoder 命令行 `UV4 -r`：0 错误，61 警告均为遗留基线，Code=44840，hex+map 已再生**；上一次为 09-08 Keil 手点 Code=44616）。改固件后必须重编译并核对 build_log。**重编译不再需要等用户手点**：`D:\ruanjian\keil\KEIL32\UV4\UV4.exe -b|-r <uvprojx> -j0 -o <log>`，退出码 0=无警无错、1=仅警告、2=有错误；核对时读 `build_log.txt` 与 `OBJ/PWM.map`（符号是否真进镜像）。
- **P0-7 刀控仍开放**：3 GPIO 不能完成 6 路换向、上电首 500ms 刀全落已实测确认——与 Hw-9/9a 继电器板接入方案一并重做（等用户端子照片）。
- **工作区已整理**（README.md 为资产位置权威，逐文件说明）：脚本在 `tools/`、标定 npz 在 `data/`、田间视频在 `data/videos/`（gitignore）；v0.7 docx 内旧路径以缺口清单 §G 勘误为准。
- **bridge 串口重组已修**（abde261 `poll_serial_frames`）：跨读边界不再丢帧，当时 34→43 例。**当前测试基线：09-14 收工 `pytest -q` = 89 collected / 89 passed**（后续每批以实测数为准，不沿用旧数）。
- **D7 代码已完成**（09-09）：`pc/status_rx.py` STATUS 接收（live 模式 UDP 9100，坏帧 try/except 计数 + 1 秒 ≥5 帧报警，6 测试），main.py HUD/CSV 已带 MCU 状态；剩实车联调验证（随 B7/B8）。
- 硬件事实（已确认）：实车板 V3.3；4×行走电机=固件同款；转向伺服在车（UART4 Modbus）；滑台伺服+丝杆在车未接线；3×电推杆+外接继电器板在车（接入方式待照片）；相机=LRCP imx291 USB UVC（MJPG 1080p30，**线材接触不良已两次实测掉设备，上车换优质 USB3.0 短线**）；车端计算=**RDK X5 已到货并验机四项全过**（OS 3.5.0，SSH sunrise，eth0 静态 192.168.127.10，40pin UART1=Pin8/10→/dev/ttyS1 3.3V；视频链路定型 TCP multipart MJPEG，网线直连实测 105ms@30fps，工具 tools/stream_view.py）。
- 下一步（细目见 `docs/缺口清单.md` 顶部『📊 总览：做完的 / 没做的』板）：**①编译门槛已过 ⇒ B1 烧录 + B2 对拍（含 SBUS 失联两子情形）现在就能做，这是解锁 H2 从「已编码」到「已台架」的唯一途径**；②**C1 秋季图像采集最紧（10 月中前）**，现在就能拍，且它是 H1.3/H1.7/H4.4 的前置；③等继电器板端子照片定刀控（P0-7/H5.4）；④用户硬件链 B6 实物确认 → E2 下单；⑤D7 随实车联调；⑥**等 zcode merge `9e5774c` + `ef7d905` 两批并回主线**，merge 前 qoder 不开涉及控制权限语义的新批。
